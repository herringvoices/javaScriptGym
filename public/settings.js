// Runtime settings loaded before the app. Hosted/prod default: use online Sandpack bundler.
window.APP_SETTINGS = {
  useLocalBundler: false, // flip to true on lab machines to force local offline bundler
  bundlerURL: "/sandpack/" // path prefix to vendored bundler assets (must contain frame.html)
};

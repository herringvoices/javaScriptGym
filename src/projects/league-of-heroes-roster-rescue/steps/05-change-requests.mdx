---
id: 05-change-requests
title: Process Roster Changes
---

# Chapter 5 - Process Roster Changes

The League finally has a roster.

Naturally, this means the roster is already out of date.

Heroes join. Heroes step away. Emergency substitutes appear for reasons nobody fully explains. Sometimes someone gets moved to the front because there is a giant robot downtown and apparently line order matters now.

The good news is that you do not need to rebuild the whole `roster` array every time something changes.

JavaScript gives arrays methods for adding and removing items from either end.

<Callout type="note" title="The roster desk">
  In Chapter 4, you created the starting roster.

  In this chapter, leave that original array declaration alone. Make changes *after* the roster exists so your code tells the story in the same order the League receives the requests.
</Callout>

## Starting Roster

Before you begin, your roster should contain these heroes in this order:

```txt
1. Scuba Driver
2. Solar Flare
3. Echo Fang
```

That means:

```txt
Index 0: Scuba Driver
Index 1: Solar Flare
Index 2: Echo Fang
```

If that is not your current roster, go back and fix Chapter 4 before continuing.

## The Tools You Need

For this chapter, you will use:

- `.push()` to add an item to the end
- `.pop()` to remove an item from the end
- `.unshift()` to add an item to the beginning
- `.shift()` to remove an item from the beginning

<Callout type="tip" title="Read the request before choosing the method">
  Do not start by asking, “Which method have I not used yet?”

  Start by asking, “Where does this hero need to go?” or “Where is this hero being removed from?”
</Callout>

## Request 1: Add a New Recruit

The League has approved a new hero: **Neon Volt**.

Create an object for Neon Volt using the same property names as the heroes already in your roster.

```txt
ID: 4
Hero name: Neon Volt
Active: true
Power level: 21
Age: 26
Power: Electro-kinesis
Secret identity: Maya Chen
City: Metroburg
```

Then add Neon Volt to the **end** of the roster.

<HandbookChallenge
  title="Add Neon Volt to the end of the roster"
  hints={[
    <>Create the hero record with the same property shape as every existing roster object.</>,
    <>The original <code>roster</code> declaration stays unchanged; this request happens afterward.</>,
    <><code>push()</code> adds an item to the end of an existing array.</>,
  ]}
  expected={
    <pre className="m-0 whitespace-pre-wrap">{`Neon Volt`}</pre>
  }
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`const neonVolt = {
  id: 4,
  heroName: "Neon Volt",
  isActive: true,
  powerLevel: 21,
  age: 26,
  power: "Electro-kinesis",
  secretIdentity: "Maya Chen",
  city: "Metroburg",
};

roster.push(neonVolt);
console.log(roster[roster.length - 1].heroName);`}</pre>
  }
>
  <p className="m-0">
    Create <code>neonVolt</code> from the profile above and add that object to the end of the existing <code>roster</code> with an array method. Then use <code>roster.length</code> to log the last hero’s name and confirm the new recruit is there.
  </p>
</HandbookChallenge>

<PracticeCard
  prompt="After adding Neon Volt, what expression gets the last hero object from the roster?"
  answer="`roster[roster.length - 1]`"
 />

## Request 2: Badge Inspection

The League's badge printer needs to inspect Neon Volt's record.

Remove the last hero from the roster and store the removed object in a variable called `badgeCheckHero`.

Then log the removed hero's name.

Your console should show:

```txt
Neon Volt
```

<HandbookChallenge
  title="Remove and save the last hero"
  hints={[
    <>The badge printer needs the object currently at the end of the array.</>,
    <><code>pop()</code> both removes the last item and returns the removed value.</>,
    <>Store that return value before reading the hero name from it.</>,
  ]}
  expected={
    <pre className="m-0 whitespace-pre-wrap">{`Neon Volt`}</pre>
  }
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`const badgeCheckHero = roster.pop();
console.log(badgeCheckHero.heroName);`}</pre>
  }
>
  <p className="m-0">
    Remove the last hero from <code>roster</code>, store the returned object in <code>badgeCheckHero</code>, and log that object’s <code>heroName</code>. The roster should temporarily contain only the original three heroes.
  </p>
</HandbookChallenge>

<Callout type="note" title="Removed does not mean destroyed">
  `.pop()` removes an item from the array, but it also gives that item back to you.

  Saving the result means Neon Volt can return after the badge check instead of vanishing into the administrative void.
</Callout>

## Request 3: Return the Recruit

The badge printer has completed its inspection and confirms that Neon Volt is, in fact, still Neon Volt.

Add `badgeCheckHero` back to the end of the roster.

Then log the roster length.

Your console should show:

```txt
4
```

## Request 4: Emergency Substitute

The League director has decided the roster needs an emergency substitute at the very front.

Create this hero:

```txt
ID: 99
Hero name: Placeholder Paladin
Active: true
Power level: 12
Age: 41
Power: Standing in Line
Secret identity: Temporary Terry
City: Clipboard City
```

Add Placeholder Paladin to the **beginning** of the roster.

<HandbookChallenge
  title="Add Placeholder Paladin to the front"
  hints={[
    <>Use the same hero-record property names as the rest of the roster.</>,
    <>This request changes the beginning of the array rather than the end.</>,
    <><code>unshift()</code> inserts an item at index <code>0</code>.</>,
  ]}
  expected={
    <pre className="m-0 whitespace-pre-wrap">{`Hero number 99 is Placeholder Paladin.`}</pre>
  }
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`const placeholderPaladin = {
  id: 99,
  heroName: "Placeholder Paladin",
  isActive: true,
  powerLevel: 12,
  age: 41,
  power: "Standing in Line",
  secretIdentity: "Temporary Terry",
  city: "Clipboard City",
};

roster.unshift(placeholderPaladin);
console.log(\`Hero number \${roster[0].id} is \${roster[0].heroName}.\`);`}</pre>
  }
>
  <p className="m-0">
    Create <code>placeholderPaladin</code> from the profile above and add that object to the beginning of <code>roster</code>. Log the hero at index <code>0</code> to confirm the substitute is first. Remember that the hero’s id is <code>99</code>; index <code>0</code> is only their current position.
  </p>
</HandbookChallenge>

## Request 5: The Substitute Is No Longer Needed

The director has found the real schedule.

Placeholder Paladin is free to return to their regularly scheduled standing-in-line duties.

Remove the first hero from the roster and store that object in a variable called `formerSubstitute`.

Then log the removed hero's name.

Your console should show:

```txt
Placeholder Paladin
```

<PracticeCard
  prompt="Which method removes the first item from an array and returns it?"
  answer="`.shift()`"
 />

## Checkpoint

After all five requests, your roster should be back to four heroes in this order:

```txt
1. Scuba Driver
2. Solar Flare
3. Echo Fang
4. Neon Volt
```

Your console should show:

```txt
4
Scuba Driver
Solar Flare
Echo Fang
Neon Volt
```

One possible set of checks:

```js
console.log(roster.length);
console.log(roster[0].heroName);
console.log(roster[1].heroName);
console.log(roster[2].heroName);
console.log(roster[3].heroName);
```

<Callout type="warn" title="Do not manually rewrite the roster">
  The final roster should be correct because you used array methods to change it.

  Do not go back to the Chapter 4 array declaration and quietly add Neon Volt there. That would make the final output look right, but you would skip the part where the roster actually responds to change.
</Callout>

<Callout type="note" title="The roster can move">
  You can now add and remove hero records from either end of the roster.

  Next, you will inspect the roster more closely and use what you know about values, methods, and data structure to prepare the League for its final audit.
</Callout>

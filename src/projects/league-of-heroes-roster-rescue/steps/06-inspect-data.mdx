---
id: 06-inspect-data
title: Inspect the League's Data
---

# Chapter 6 - Inspect the League's Data

The League roster is updated. Nobody has screamed. The spreadsheet printer is quiet.

These are all encouraging signs.

Before the League creates its audit report, it wants evidence. Not vibes. Not “I think it works.” Evidence.

In this chapter, you will inspect the roster directly, use object utilities to look inside hero records, and clean up values imported from another system.

<Callout type="note" title="What you are practicing">
  The roster already exists. You are not rebuilding it in this chapter.

  You are inspecting its data, cleaning text, converting imported values into usable numbers, and accessing information from the beginning, middle, and end of an array.
</Callout>

## Start a League Data Check

At the bottom of `main.js`, create a new section and log a heading:

```js
console.log("League Data Check");
```

Then log the number of heroes currently in `roster`.

```txt
League Data Check
4
```

<HandbookChallenge
  title="Count the Roster"
  hints={[
    "The `roster` array already exists from Chapter 5.",
    "Arrays have a property that tells you how many items they contain."
  ]}
>
  Log the number of heroes currently in `roster`.
</HandbookChallenge>

<PracticeCard
  prompt="What should `roster.length` be after Chapter 5?"
  answer="`4`, because the final roster contains Scuba Driver, Solar Flare, Echo Fang, and Neon Volt."
/>

## Inspect One Hero Record

Use `Object.keys()` with Scuba Driver's object to inspect the property names in the first roster record.

```txt
id,heroName,isActive,powerLevel,age,power,secretIdentity,city
```

Then use `Object.values()` with the same record.

```txt
1,Scuba Driver,true,10,21,Underwater Transportation,,Old York
```

That blank space between the commas represents Scuba Driver's `null` secret identity. It is still there. It is just not much of a conversationalist.

<HandbookChallenge
  title="Inspect the First Record"
  hints={[
    "Scuba Driver is the first hero in `roster`.",
    "`Object.keys()` gives you property names.",
    "`Object.values()` gives you the values stored in those properties."
  ]}
>
  Log Scuba Driver's property names and values using object utilities.
</HandbookChallenge>

<Callout type="tip" title="A consistent shape is useful">
  Every hero record should use the same property names. That way, future code can ask any hero for `heroName`, `powerLevel`, or `city`.
</Callout>

## Inspect a Property and Its Value

Use `Object.entries()` with Scuba Driver's object, then log the first entry from the result.

```txt
id,1
```

<details>
<summary>What is happening here?</summary>

`Object.entries(roster[0])` creates an array of smaller arrays. The first one is:

```js
["id", 1]
```

When JavaScript logs that array, it displays `id,1`.

</details>

<PracticeCard
  prompt="Which expression gets the first property/value pair from Scuba Driver's object?"
  answer="`Object.entries(roster[0])[0]`"
/>

## Check the Roster Positions

Log these hero names:

- the first hero
- the second hero
- the last hero using a direct index
- the last hero using `.length - 1`

```txt
Scuba Driver
Solar Flare
Neon Volt
Neon Volt
```

<HandbookChallenge
  title="Check the Lineup"
  hints={[
    "The first hero is at index `0`.",
    "The second hero is at index `1`.",
    "Neon Volt is currently at index `3`.",
    "The last item can also be found with `array.length - 1`."
  ]}
>
  Log the requested hero names using both direct indexes and `.length - 1`.
</HandbookChallenge>

<Callout type="warn" title="Indexes can change">
  `roster[3]` works right now because Neon Volt is fourth. `roster[roster.length - 1]` is more flexible when you mean “whoever is last.”
</Callout>

## Clean Imported Text

The League recovered a few values from Dr. Breach's extremely questionable backup system.

```js
const importedCodename = "  neon-volt  ";
const importedPowerLevel = "21 points";
const importedBadgeScore = "91.5";
```

The press team needs this codename:

```txt
NEON VOLT
```

Create `cleanCodename`. Use string methods to remove extra spaces, replace the dash with a space, and convert the result to uppercase. Then log it.

<HandbookChallenge
  title="Clean the Codename"
  hints={[
    "Start with `.trim()`.",
    "Use `.replace()` to change the dash into a space.",
    "Use `.toUpperCase()` after cleaning the text."
  ]}
>
  Create and log `cleanCodename` so the imported codename becomes `NEON VOLT`.
</HandbookChallenge>

## Parse Imported Numbers

The imported power level looks like a number, but JavaScript still sees it as text.

Create `parsedPowerLevel` with `parseInt()`, then log it.

```txt
21
```

Next, create `parsedBadgeScore` with `parseFloat()` so the decimal remains intact.

```txt
91.5
```

<HandbookChallenge
  title="Parse the Imports"
  hints={[
    "`parseInt()` is useful when you need a whole number from text.",
    "`parseFloat()` is useful when the number may include a decimal.",
    "Use the parsed values in the next step instead of the original strings."
  ]}
>
  Parse the imported power level and badge score, then log both parsed values.
</HandbookChallenge>

<Callout type="note" title="Looks can be deceiving">
  `"21"` and `21` look almost identical when they print to the console. A string is text; a number can participate in numeric calculations and comparisons.
</Callout>

## Use a Parsed Value

Check whether the parsed power level is greater than Scuba Driver's power level.

```txt
true
```

<HandbookChallenge
  title="Compare the Power Levels"
  hints={[
    "Scuba Driver's power level is in the first object in `roster`.",
    "Use the parsed number, not the original imported string.",
    "A comparison expression produces a boolean value."
  ]}
>
  Log whether the parsed imported power level is greater than Scuba Driver's power level.
</HandbookChallenge>

## Checkpoint

```txt
League Data Check
4
id,heroName,isActive,powerLevel,age,power,secretIdentity,city
1,Scuba Driver,true,10,21,Underwater Transportation,,Old York
id,1
Scuba Driver
Solar Flare
Neon Volt
Neon Volt
NEON VOLT
21
91.5
true
```

<Callout type="note" title="The data is ready to inspect">
  You can now check an array's size, inspect an object's structure, access records by position, clean imported text, and convert imported values into real numbers.

  Next, the League will use that information to build the roster audit it has been asking about since Chapter 1.
</Callout>

// Deprecated: Sandpack bundler selection has been removed.
// This module is kept as a stub to avoid breaking imports in older branches.
// New Monaco-based pipeline does not use any external bundler.
export function getBundlerURL() {
  return undefined;
}

---
id: overview
title: How to use the handbook
short: Read this first when onboarding to the practice playground.
---
# Welcome to the JavaScript Gym Handbook!

This handbook is your **intro to JavaScript book** for the playground.
Each chapter focuses on a single standard (like arrays, functions, or iteration),
and each standard has matching practice tickets on the **Challenges** page.

The rhythm we’re aiming for is:

1. **Read a chapter** here in the handbook.
2. **Practice that standard** on the Challenges page.
3. **Come back to the chapter** if you need a refresh.

Over time, you’ll loop through this pattern across standards:

- Value data types → collections → methods → control flow → functions → iteration, and so on.
- Each chapter builds on the last.

---

## How to pair the handbook with challenges

Here’s how to use the site now that the handbook is a first-class citizen:

1. **Pick a chapter here** that matches a standard, like `JS.PF.ITR` for iteration.
2. **Read the overview** and work through the inline HandbookChallenges. These are short, focused reps using the editor + console.
3. **Switch to the Challenges page** and filter by that same standard. Do the practice tickets that reinforce what you just read.
4. **Return to the handbook** when something feels fuzzy, or when you're ready for a new chapter.

You can absolutely skim ahead, but when in doubt:

- Let the **handbook explain the idea**.
- Let the **challenges stress-test your understanding**.

The fastest progress comes from bouncing between the two.

---
## What JavaScript is (and isn’t)

JavaScript isn’t magic. It can feel that way at first, but it’s really a toolbox for manipulating data.

- Posting on Instagram → Create data
- Scrolling your feed → Read data
- Changing your username → Update data
- Deleting a post → Delete data

That’s CRUD, and it’s most of what we do. Our goal here isn’t to teach you every tool in the JavaScript toolbox. It’s to teach you the basic tools you’ll actually use—and when to use them.

## Handbook Challenges

Throughout the handbook, you’ll reach **YOUR TURN** checkpoints. When you see one, stop reading and do the task in the editor before you continue.

<HandbookChallenge title="Try a handbook challenge">
  <p className="m-0">
    This is the shape you’ll see throughout the handbook: a clear task, optional help if you get stuck, and a solution you can compare against after you try.
  </p>
</HandbookChallenge>

The title tells you the programming action you’re practicing. When a task has a specific observable result, the challenge will show that separately. If you need help, hints appear one at a time so you can take the smallest nudge you need. The solution stays separate from the hints.

This is important: the only way to learn is to do. If you aren't _practicing_ these concepts, you may _feel_ like you get them in the moment, but they won't stick.

You also might notice that, unlike a lot of online programming tutorials, we don't have any tests to run to see if your code is doing what it's supposed to. Instead, you will use your eyes to see if the result you get is the result you expect. I've seen too many students chasing checkmarks on a test instead of using their eyes and their brain to figure out what went wrong.

Finally, don't feel limited to doing only what the challenges say! Explore, try new things, break old ones! Breaking things is one of the best ways to learn JavaScript.

<Callout type="note" title="A quick editor note">

The workspace opens on `index.html` first because it is the page your code runs inside. We won't be worrying about our HTML for a long time, so until you're told otherwise, you should click "Show Files" in the workspace editor, and then click `main.js` in the file tree that opens up. That is where your JavaScript belongs.

</Callout>

## Your first tool: <code>console.log</code>

Think of <code>console.log</code> as your stethoscope: it lets you hear what your code is doing.

```js
console.log("Hello, handbook!");
```

You’ll use it constantly to check values, confirm what ran, and debug surprises.

<HandbookChallenge
  title="Experiment with console output"
  hints={[
    <>Text literals need quotation marks so JavaScript treats them as strings.</>,
    <>JavaScript runs these statements from top to bottom, so changing line order changes output order.</>,
    <>Without quotes, <code>first</code> is treated as an identifier. An identifier that does not exist produces a <code>ReferenceError</code>.</>,
  ]}
  expected={
    <pre className="m-0 whitespace-pre-wrap">{`first
second
third`}</pre>
  }
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`console.log("first");
console.log("second");
console.log("third");`}</pre>
  }
>
  <ol className="m-0 space-y-1 pl-5">
    <li>Log three words on three lines: <code>"first"</code>, <code>"second"</code>, <code>"third"</code>.</li>
    <li>Run and check the console.</li>
    <li>Reorder the lines and run again. Notice what changes.</li>
    <li>Remove the quotes from one line, such as <code>console.log(first)</code>. Run it again and inspect the error.</li>
  </ol>
</HandbookChallenge>

---

## How JavaScript runs (top → bottom)

JavaScript reads your file top to bottom, one line at a time. If line 3 uses something that isn’t defined until line 20, you’ll get an error.

```js
console.log(greeting); // ❌ ReferenceError: Cannot access 'greeting' before initialization
const greeting = "hi";
```

We’ll learn patterns that run code later (events, functions), but for now think of your file like a book: top → bottom.

<HandbookChallenge
  title="Define a value before using it"
  hints={[
    <>JavaScript executes these two statements from top to bottom.</>,
    <>The declaration has to run before the line that reads <code>greeting</code>.</>,
  ]}
  expected={
    <pre className="m-0 whitespace-pre-wrap">{`hi`}</pre>
  }
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`const greeting = "hi";
console.log(greeting);`}</pre>
  }
>
  <p className="m-0">Paste this code into the editor and run it:</p>

  <pre className="m-0 whitespace-pre-wrap">{`console.log(greeting);
const greeting = "hi";`}</pre>

  <p className="m-0">
    Then rearrange the two lines so the value exists before JavaScript tries to log it. Run the code again and confirm the output.
  </p>
</HandbookChallenge>

---

## Comments: notes for humans (and how to toggle them)

Programs are for computers and for people. Comments are short notes the computer ignores but teammates (and future-you) will appreciate.

- Line comment: anything after <code>//</code> on a line is ignored
- Toggle quickly: press <strong>Ctrl+/</strong> (Windows/Linux) or <strong>Cmd+/</strong> (Mac) to comment/uncomment selected lines

```js
console.log("Visible");
// console.log("Hidden for now"); // toggled off with Ctrl+/ (Cmd+/ on Mac)
console.log("Still visible"); // end-of-line comment is fine, too
```

<HandbookChallenge
  title="Comment out a broken line"
  hints={[
    <>A line that begins with <code>//</code> is ignored by JavaScript.</>,
    <>The editor shortcut toggles the selected line between active code and a comment.</>,
  ]}
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`// console.log(first);`}</pre>
  }
>
  <ol className="m-0 space-y-1 pl-5">
    <li>Reproduce the earlier error by removing quotes: <code>console.log(first)</code>.</li>
    <li>Select that broken line and use <strong>Ctrl+/</strong> (or <strong>Cmd+/</strong>) to comment it out.</li>
    <li>Run again. The commented line should no longer execute.</li>
    <li>Toggle it again to bring the line back.</li>
  </ol>
</HandbookChallenge>

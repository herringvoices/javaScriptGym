---
id: JS.VDT.PRM.STR
title: Strings · Text Data
short: Create text values, join them, and embed variables with template literals.
---

# Data Types → Strings

Variables can hold lots of different types of data. We call those different types of data... **data types**. Because we are very clever and good at naming things.

The simplest types are called **primitives**: **string**, **number**, **boolean**, **null**, and **undefined**.

In this chapter we’re starting with **strings**—just text.

<Callout type="note" title="Strings, in one breath">
A <strong>string</strong> is text. Write it in quotes, like <code>"Hello world"</code>. For consistency, we’ll use <strong>double quotes</strong> in this course. Later you’ll also see single quotes and backticks.
</Callout>
<br/>
<Callout type="note" title="Movie Night Planner checkpoint">
  <p className="m-0">Bring the variables you created in the Variables chapter with you. Your editor should already contain the core Movie Night Planner setup:</p>
  <pre className="m-0 whitespace-pre-wrap">{`let viewerName = "Milo";
let currentMood = "excited";
const location = "living room";
const streamingService = "CouchFlix";`}</pre>
  <p className="m-0">If not, clear your file and paste this snippet (swap in your own values). Every string challenge below assumes this baseline.</p>
</Callout>

## 1) Your first strings

<Callout type="tip" title="Problem this solves">
When you write <code>console.log(Nashville)</code>, JavaScript goes “Who’s Nashville?” and throws a <em>ReferenceError</em>. We need a way to say “this is <em>text</em>, not a variable name.” Quotes do that.
</Callout>

```js
const firstName = "Riley";
const hometown = "Nashville";
console.log(firstName, hometown); // Riley Nashville
```

<HandbookChallenge
  title="Add the show and snack"
  hints={[
    <>Strings are text values wrapped in quotation marks.</>,
    <>Use <code>const</code> for the show because this plan treats it as fixed, and <code>let</code> for the snack because it may change.</>,
    <>Log both variable names together to inspect both stored strings.</>,
  ]}
  expected={
    <pre className="m-0 whitespace-pre-wrap">{`Mystery Manor kettle corn`}</pre>
  }
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`const favoriteShow = "Mystery Manor";
let snack = "kettle corn";

console.log(favoriteShow, snack);`}</pre>
  }
>
  <p className="m-0">
    Keep the Movie Night Planner variables already in your editor. Add <code>favoriteShow</code> as a <code>const</code> string with the value <code>"Mystery Manor"</code> and <code>snack</code> as a <code>let</code> string with the value <code>"kettle corn"</code>. Log both values on one line.
  </p>
</HandbookChallenge>

---

## 2) Gluing text together with <code>+</code> (concatenation)

<Callout type="tip" title="Problem this solves">
You can log pieces separately, but humans read <em>sentences</em>, not piles of words. We need a way to stitch multiple strings into one line.
</Callout>

The <code>+</code> operator <strong>concatenates</strong>—it squishes two strings together and glues them. You control spacing and punctuation.

```js
const animal = "otter";
const mood = "sleepy";
const sentence = "The " + animal + " looks " + mood + ".";
console.log(sentence); // "The otter looks sleepy."
```

<HandbookChallenge
  title="Build the invite with concatenation"
  hints={[
    <>Concatenation joins each string and variable in the order you write them.</>,
    <>Spaces and punctuation are characters too, so include them inside the quoted pieces where needed.</>,
    <>Use <code>+</code> between each piece of the final sentence.</>,
  ]}
  expected={
    <pre className="m-0 whitespace-pre-wrap">{`Join Milo in the living room to stream Mystery Manor with kettle corn.`}</pre>
  }
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`const inviteLine = "Join " + viewerName + " in the " + location +
  " to stream " + favoriteShow + " with " + snack + ".";

console.log(inviteLine);`}</pre>
  }
>
  <Callout type="note" title="Starting point">
    <p className="m-0">Keep <code>viewerName</code>, <code>favoriteShow</code>, <code>snack</code>, and <code>location</code> in your editor.</p>
  </Callout>
  <p className="m-0">
    Create <code>inviteLine</code> by concatenating the existing values with quoted text and <code>+</code>. The finished sentence should invite the viewer to the location to stream the favorite show with the chosen snack. Log the result.
  </p>
</HandbookChallenge>

<br/>

<Callout type="danger" title="But… this gets old fast">
With a few pieces, <code>+</code> is fine. With four or five, it turns into quotes‑and‑pluses spaghetti. Miss one space and you get <code>"HelloRiley"</code>. Wouldn’t it be nice if there were an easier way? Maybe one with an even more obnoxious name than "concatenation"??
</Callout>

---

## 3) Interpolation (insert values into a string)

<Callout type="tip" title="Problem this solves">
Concatenation works, but it’s fragile and hard to read with many parts. We want a cleaner way to write sentences without babysitting spaces. Enter <strong>string interpolation</strong>—yes, that gloriously obnoxious name.
</Callout>

In JavaScript, to do string interpolation, we use <strong>backticks</strong> (the key under ESC) to let us embed variables with <code>$\{...\}</code>. You'll also hear these called <strong>template literals</strong>.

```js
const drink = "chai latte";
const cafe = "River & Rye";
const line = `Order ready: ${drink} at ${cafe}.`;
console.log(line); // "Order ready: chai latte at River & Rye."
```

<Callout type="danger" title="Backticks, not quotes">
Interpolation only works inside <strong>backticks</strong> ( <code>`</code> ). <code>"Hi $\{name\}"</code> won’t interpolate; you’ll just see the symbols.
</Callout>
<br/>
<HandbookChallenge
  title="Rewrite the invite with interpolation"
  hints={[
    <>Template literals use backticks instead of ordinary quotation marks.</>,
    <>Interpolation slots insert existing values without breaking the sentence into separate strings.</>,
    <>A slot follows the pattern <code>${'{'}variableName{'}'}</code>.</>,
  ]}
  expected={
    <pre className="m-0 whitespace-pre-wrap">{`Tonight, Milo is streaming Mystery Manor in the living room with kettle corn ready on CouchFlix.`}</pre>
  }
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`const inviteMessage = \`Tonight, \${viewerName} is streaming \${favoriteShow} in the \${location} with \${snack} ready on \${streamingService}.\`;

console.log(inviteMessage);`}</pre>
  }
>
  <p className="m-0">
    Rebuild the invite as a template literal named <code>inviteMessage</code>. Include <code>viewerName</code>, <code>favoriteShow</code>, <code>location</code>, <code>snack</code>, and <code>streamingService</code> with interpolation, then log the result. Keep <code>inviteLine</code> in your file so you can compare the two approaches.
  </p>
</HandbookChallenge>
<br/>

<Callout type="tip" title="Strings checkpoint">
  <p className="m-0">Before heading to numbers, keep this snapshot (customize values) so the next chapter starts from the same place:</p>
  <pre className="m-0 whitespace-pre-wrap">{`let viewerName = "Milo";
let currentMood = "excited";
const location = "living room";
const streamingService = "CouchFlix";
const favoriteShow = "Mystery Manor";
let snack = "kettle corn";

const inviteLine = "Join " + viewerName + " in the " + location +
  " to stream " + favoriteShow + " with " + snack + ".";
const inviteMessage = \`Tonight, $\{viewerName\} is streaming $\{favoriteShow\}
  in the $\{location\} with $\{snack\} ready on $\{streamingService\}.\`;

console.log(inviteMessage);`}</pre>
  <p className="m-0">If things feel off, paste this snippet into your editor before moving on.</p>
</Callout>

---

## Common gotchas (quick fixes)

* **Missing spaces:** If your sentence looks jammed, add spaces inside your strings: <code>"Hello " + name</code>.
* **Wrong quotes for interpolation:** Use backticks for <code>$\{...\}</code>.
* **Mismatched quotes:** Start and end with the same kind (we’ll default to double quotes).

---

## Technical Vocabulary

| Term             | Plain-English meaning                                  | Tiny example                              |
| :--------------- | :----------------------------------------------------- | :---------------------------------------- |
| string           | Text inside quotes                                     | <code>"Nashville"</code>                  |
| concatenate      | Glue strings with <code>+</code>                       | <code>"Hi " + "there"</code>              |
| template literal | Backtick string with <code>$\{...\}</code> slots | <code>`Name: ${firstName}`</code> |
| backtick         | Character used for template literals                   | <code>`like this`</code>                  |
| interpolation    | Inserting values inside a backtick string              | <code>`Hello, ${name}`</code>     |

---

## Wrap-up

* Strings are just <strong>text in quotes</strong>.
* Use <strong>`+`</strong> to glue strings together, but it gets messy—<strong>template literals</strong> (backticks + <code>$\{...\}</code>) are cleaner.

<strong>Next up: Numbers.</strong> Operators, math, and the little gotchas when you mix strings with numbers (spoiler: you shouldn’t—yet).

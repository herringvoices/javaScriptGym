---
id: JS.VDT.PRM.NUM
title: Numbers · Counting & Simple Math
short: Make numeric values, add/subtract/multiply/divide, and update totals with compound assignment.
---

# Data Types → Numbers

Last time, we worked with **strings**—text in quotes—so we could label things and build readable sentences. Strings are great for names, messages, and anything you’d say out loud.

But sentences don’t add up. To do *real math*, we need our second data type: **numbers**. Now, I know some of us hear "math" and run screaming for the hills, but this won't be anything too scary—just the kind of simple math your apps actually use.

Consider these problems:

* A customer wants to see the **total** for their order.
* A player **adds points** to their score in a web game.
* A theater shows how many **seats are left** in a row.
* A timer **counts down** to the start of class.

That’s numbers territory: counts, totals, and simple scaling. No calculus. No proofs. Just values you can calculate with so your app can answer “how many?” and “what’s the total?” without breaking a sweat.

<Callout type="note" title="Movie Night Planner checkpoint">
  <p className="m-0">Keep the script you built in Strings. You’ll need these variables before writing any numbers:</p>
  <pre className="m-0 whitespace-pre-wrap">{`let viewerName = "Milo";
let currentMood = "excited";
const location = "living room";
const streamingService = "CouchFlix";
const favoriteShow = "Mystery Manor";
let snack = "kettle corn";

const inviteLine = "Join " + viewerName + " in the " + location +
  " to stream " + favoriteShow + " with " + snack + ".";
const inviteMessage = \`Tonight, \$\{viewerName\} is streaming \$\{favoriteShow\}
  in the \$\{location\} with \$\{snack\} ready on \$\{streamingService\}.`}</pre>
  <p className="m-0">If your file looks different, reset it to this baseline (customize the values) before adding the next lines.</p>
</Callout>

## 1) Your first numbers

To make a number, you just type a numeral without quotation marks. For example, `5` instead of `"5"`.

Let's take a look at some numbers being assigned to variables:

```js
const redTeam = 5;
const blueTeam = 4;
console.log(redTeam, blueTeam); // 5 4
```

<HandbookChallenge
title="Count the guest list"
hints={[
"Keep your string variables above this section and add numbers underneath them.",
"Use multiplication to calculate totals instead of doing the math in your head.", <pre key="peek" className="m-0 whitespace-pre-wrap">{`let numGuests = 3;
const snacksPerGuest = 2;
const drinksPerGuest = 1;
let totalSnacksNeeded = numGuests * snacksPerGuest;
let totalDrinksNeeded = numGuests * drinksPerGuest;
console.log(totalSnacksNeeded, totalDrinksNeeded);`}</pre>,
]}
answers={ <pre className="m-0 whitespace-pre-wrap">{`let numGuests = 3;
const snacksPerGuest = 2;
const drinksPerGuest = 1;

let totalSnacksNeeded = numGuests * snacksPerGuest;
let totalDrinksNeeded = numGuests * drinksPerGuest;
console.log(totalSnacksNeeded, totalDrinksNeeded); // 6 3`}</pre>
}
>

	<p className="m-0">
		Extend the Movie Night Planner by storing how many guests are coming plus how many snacks and drinks each person needs. Multiply to compute <code>totalSnacksNeeded</code> and <code>totalDrinksNeeded</code>, then log both numbers.
		</p>
	<p className="mt-3 mb-1 text-slate-300"><strong>Example console output:</strong></p>
	<pre className="m-0 whitespace-pre-wrap">{`6 3`}</pre>
</HandbookChallenge>

---

## 2) Addition & subtraction

```js
let total = 0;      // start at zero
total = total + 3;  // add three
total = total - 1;  // subtract one
console.log(total); // 2
```

### Compound assignment (operators + the assignment operator)

We already learned that **`=`** is the assignment operator: **evaluate the right, then store in the left** (one box name on the left, a value on the right).

Compound assignment just **combines an operator with `=`** so you can update a variable in place.

**Rule for `=`:**
**“Evaluate the expression on the right down to _one_ value, then store that value in the variable on the left.”**

**For `+=`:**
**“Take the value on the right, add it to the value on the left, and store it.”**

```js
let total = 0;

total += 3;  // add and assign (same as total = total + 3)
total -= 1;  // subtract and assign
console.log(total); // 2
```

**Quick map**

* `x += y` → take left, **add** right, store in left
* `x -= y` → take left, **subtract** right, store in left
* `x *= y` → take left, **multiply by** right, store in left
* `x /= y` → take left, **divide by** right, store in left

> Same rule as before: JavaScript evaluates the **right-hand side first**, then assigns the result to the **single** variable on the left.

<HandbookChallenge
title="RSVP ripple"
hints={[
"Re-use <code>numGuests</code>, <code>snacksPerGuest</code>, and <code>drinksPerGuest</code> from the previous challenge.",
"Every time the guest count changes, recompute the totals so the planner doesn’t drift.", <pre key="peek" className="m-0 whitespace-pre-wrap">{`numGuests += 1;
totalSnacksNeeded = numGuests * snacksPerGuest;
totalDrinksNeeded = numGuests * drinksPerGuest;
console.log(numGuests, totalSnacksNeeded);

numGuests -= 2;
totalSnacksNeeded = numGuests * snacksPerGuest;
totalDrinksNeeded = numGuests * drinksPerGuest;
console.log(numGuests, totalSnacksNeeded);`}</pre>,
]}
answers={ <pre className="m-0 whitespace-pre-wrap">{`numGuests += 1; // a friend brings a plus-one
totalSnacksNeeded = numGuests * snacksPerGuest;
totalDrinksNeeded = numGuests * drinksPerGuest;
console.log(numGuests, totalSnacksNeeded, totalDrinksNeeded);

numGuests -= 2; // two people drop out
totalSnacksNeeded = numGuests * snacksPerGuest;
totalDrinksNeeded = numGuests * drinksPerGuest;
console.log(numGuests, totalSnacksNeeded, totalDrinksNeeded);`}</pre>
}
>

	<Callout type="note" title="Starting point">
		<p className="m-0">Your editor should already contain <code>numGuests</code>, <code>snacksPerGuest</code>, <code>drinksPerGuest</code>, and the running totals.</p>
	</Callout>
	<p className="m-0">
		Use <code>+=</code> and <code>-=</code> to react when guests RSVP yes/no. After each change, recompute <code>totalSnacksNeeded</code> and <code>totalDrinksNeeded</code>, then log the new counts.
		</p>
	<p className="mt-3 mb-1 text-slate-300"><strong>Example console output:</strong></p>
	<pre className="m-0 whitespace-pre-wrap">{`4 8 4
2 4 2`}</pre>
</HandbookChallenge>

---

## 2.5) Numbers vs strings: 2 vs "2"

<Callout type="caution" title="Same plus sign, different meaning">
If either side is a <strong>string</strong>, JavaScript will treat <code>+</code> as <strong>concatenate</strong> and glue text together. If both sides are <strong>numbers</strong>, <code>+</code> does <strong>addition</strong>.
</Callout>

```js
console.log("2" + "2"); // "22" (two strings concatenated)
console.log(2 + 2);       // 4   (two numbers added)
```

<HandbookChallenge
title="Label or add?"
hints={[
"Log both the numeric math and the string version so you can see the difference.",
"You already have <code>numGuests</code> and <code>totalSnacksNeeded</code> from earlier.", <pre key="peek" className="m-0 whitespace-pre-wrap">{`console.log(numGuests + 2); // numeric addition
console.log(numGuests + " guests confirmed"); // string concatenation
console.log("Snacks: " + totalSnacksNeeded);`}</pre>,
]}
answers={ <pre className="m-0 whitespace-pre-wrap">{`console.log(numGuests + 2); // adds 2 more guests numerically
console.log(numGuests + " guests confirmed"); // turns the number into text
console.log("Snacks: " + totalSnacksNeeded);`}</pre>
}
>

	<p className="m-0">
		Log three lines: the numeric total if two more people show up, a string label like <code>"4 guests confirmed"</code>, and another label for snacks. Notice how the same <code>+</code> either adds or concatenates depending on whether you involve a string.
		</p>
	<p className="mt-3 mb-1 text-slate-300"><strong>Example console output:</strong></p>
	<pre className="m-0 whitespace-pre-wrap">{`5
3 guests confirmed
Snacks: 6`}</pre>
</HandbookChallenge>

For now, keep it simple: **quotes = text** (glue with `+`), **no quotes = numbers** (add with `+`). We'll explore mixing types and coercion later.

---

## 3) Multiplication & division

```js
const seatsPerRow = 8;
const rows = 6;
const seats = seatsPerRow * rows;
console.log(seats); // 48
```

```js
const cookies = 24;
const kids = 6;
const cookiesEach = cookies / kids;
console.log(cookiesEach); // 4
```

<HandbookChallenge
title="Snack stock math"
hints={[
"Introduce <code>snacksOnHand</code> so you can compare inventory to the total needed.",
"Use division to estimate how many bowls you’ll fill (e.g., 4 snacks fit in one bowl).", <pre key="peek" className="m-0 whitespace-pre-wrap">{`let snacksOnHand = 10;
const snacksPerBowl = 4;
let bowlsNeeded = totalSnacksNeeded / snacksPerBowl;
console.log(bowlsNeeded);

snacksOnHand += 2; // grabbed another bag
const snacksRemaining = snacksOnHand - totalSnacksNeeded;
console.log(snacksRemaining);`}</pre>,
]}
answers={ <pre className="m-0 whitespace-pre-wrap">{`let snacksOnHand = 10;
const snacksPerBowl = 4;
let bowlsNeeded = totalSnacksNeeded / snacksPerBowl;
console.log(bowlsNeeded); // 1.5 if you need 6 snacks

snacksOnHand += 2;
const snacksRemaining = snacksOnHand - totalSnacksNeeded;
console.log(snacksRemaining);`}</pre>
}
>

	<p className="m-0">
		Track how many snacks you already have (<code>snacksOnHand</code>), calculate how many bowls you need with division, then update <code>snacksOnHand</code> if you buy more. Finally, subtract to see how many snacks remain after covering guests.
		</p>
	<p className="mt-3 mb-1 text-slate-300"><strong>Example console output:</strong></p>
	<pre className="m-0 whitespace-pre-wrap">{`1.5
6`}</pre>
</HandbookChallenge>
<br/>

<Callout type="tip" title="Numbers checkpoint">
  <p className="m-0">Before moving to booleans, your script should now look something like this (customize the values):</p>
  <pre className="m-0 whitespace-pre-wrap">{`let viewerName = "Milo";
let currentMood = "excited";
const location = "living room";
const streamingService = "CouchFlix";
const favoriteShow = "Mystery Manor";
let snack = "kettle corn";

const inviteLine = "Join " + viewerName + " in the " + location +
  " to stream " + favoriteShow + " with " + snack + ".";
const inviteMessage = \`Tonight, \$\{viewerName\} is streaming \$\{favoriteShow\}
  in the \$\{location\} with \$\{snack\} ready on \$\{streamingService\}.\`;

let numGuests = 3;
const snacksPerGuest = 2;
const drinksPerGuest = 1;
let totalSnacksNeeded = numGuests * snacksPerGuest;
let totalDrinksNeeded = numGuests * drinksPerGuest;
let snacksOnHand = 10;
const snacksPerBowl = 4;
let bowlsNeeded = totalSnacksNeeded / snacksPerBowl;`}</pre>
  <p className="m-0">If anything is missing, reset your editor to this snapshot before starting the booleans chapter.</p>
</Callout>

---

## 4) Order of operations (PEMDAS, lightly)

Multiplication and division are performed before addition and subtraction. To change that order, wrap the part you want evaluated first in parentheses—for example, write `(2 + 3) * 4` to do the addition before multiplying.

```js
console.log(2 + 3 * 4);   // 14
console.log((2 + 3) * 4); // 20
```

---

## Common gotchas (quick fixes)

* Don’t mix numbers with text (e.g., `"2" + 2` sticks them together as text).
* You can use decimals as well, but sometimes JavaScript does weird things with decimal math (e.g., `0.1 + 0.2` results in `0.30000000000000004`).
* Remember the assignment rule: **evaluate the right, then store in the left**.

---

## Technical Vocabulary

| Term                | Plain-English meaning             | Tiny example   |
| ------------------- | --------------------------------- | -------------- |
| number              | A numeric value (no quotes)       | `42`           |
| operator            | A symbol that transforms values   | `+ - * /`      |
| expression          | Code that produces a value        | `a + b * 2`    |
| assignment operator | Puts a value into a variable      | `x = 10`       |
| compound assignment | Operator + `=` to update in place | `total += 3`   |
| initialize          | Declare and assign in one step    | `let n = 0`    |
| precedence          | Order that operations run         | `*` before `+` |
| parentheses         | Force the order you mean          | `(2 + 3) * 4`  |

---

## Wrap-up

You created and updated numeric values with **`+`**, **`-`**, **`*`**, **`/`**, and learned **compound assignment** (your new best friend for running totals).

**Next up: Booleans.** True/false values that power decisions.
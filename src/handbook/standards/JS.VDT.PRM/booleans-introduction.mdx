---
id: JS.VDT.PRM.BOOL
title: Booleans · True/False & Empty Values
short: Ask clear yes/no questions with comparisons; use null/undefined to represent empty states on purpose vs by accident.
---

# Data Types → Booleans (plus null and undefined)

Strings let us **describe** the Movie Night Planner. Numbers let us **count** guests, snacks, and bowls. Now we need a way to **decide** whether things are ready. That job belongs to the **boolean** type: a light switch that’s either **on (`true`)** or **off (`false`)**—no dimmers, no maybes.

<Callout type="note" title="Numbers checkpoint">
  <p className="m-0">Your code should look something like this by the end of the Numbers chapter (customize the values, but keep the structure):</p>
  <pre className="m-0 whitespace-pre-wrap">{`let viewerName = "Milo";
let currentMood = "excited";
const location = "living room";
const streamingService = "CouchFlix";
const favoriteShow = "Mystery Manor";
let snack = "kettle corn";

const inviteLine = "Join " + viewerName + " in the " + location +
  " to stream " + favoriteShow + " with " + snack + ".";
const inviteMessage = \`Tonight, \$\{viewerName\} is streaming \$\{favoriteShow\}
  in the \$\{location\} with \$\{snack\} ready on \$\{streamingService\}.\`;

let numGuests = 3;
const snacksPerGuest = 2;
const drinksPerGuest = 1;
let totalSnacksNeeded = numGuests * snacksPerGuest;
let totalDrinksNeeded = numGuests * drinksPerGuest;
let snacksOnHand = 10;
const snacksPerBowl = 4;
let bowlsNeeded = totalSnacksNeeded / snacksPerBowl;`}</pre>
  <p className="m-0">We’ll layer booleans on top of these numbers to see whether the plan is actually ready.</p>
</Callout>

---

## 1) Status lights for the plan (literal booleans)

Sometimes you already know the answer to a yes/no question. Store it directly as <code>true</code> or <code>false</code>.

```js
let hasSentInvites = false;
const captionsEnabled = true;
console.log(hasSentInvites, captionsEnabled); // false true
```

Name booleans so you can read them out loud: “<em>has sent invites</em> is false” makes sense. Avoid double negatives like <code>isNotReady</code>—your future self will thank you.

<HandbookChallenge
  title="Set the status lights"
  hints={[
    "Keep your strings and numbers above this new section so every chapter shares the same script.",
    "Use <code>true</code>/<code>false</code> directly—no quotes.",
    <pre key="peek" className="m-0 whitespace-pre-wrap">{`let hasSentInvites = false;
let isProjectorOn = false;
const captionsEnabled = true;
console.log(hasSentInvites, isProjectorOn, captionsEnabled);
hasSentInvites = true;
console.log(hasSentInvites, isProjectorOn, captionsEnabled);`}</pre>,
  ]}
  answers={
    <pre className="m-0 whitespace-pre-wrap">{`let hasSentInvites = false;
let isProjectorOn = false;
const captionsEnabled = true;
console.log(hasSentInvites, isProjectorOn, captionsEnabled); // false false true
hasSentInvites = true;
console.log(hasSentInvites, isProjectorOn, captionsEnabled); // true false true`}</pre>
  }
>
  <p className="m-0">
    Add three booleans to your planner (<code>hasSentInvites</code>, <code>isProjectorOn</code>, <code>captionsEnabled</code>). Log them once, flip <code>hasSentInvites</code> to <code>true</code> to simulate blasting the invites, then log again.
  </p>
  <p className="mt-3 mb-1 text-slate-300"><strong>Example console output:</strong></p>
  <pre className="m-0 whitespace-pre-wrap">{`false false true
true false true`}</pre>
</HandbookChallenge>

---

## 2) Comparisons: ask the numbers yes/no questions

We don’t want to hand-type <code>true</code> and <code>false</code> for every scenario. Instead, we <strong>derive booleans</strong> by asking the numbers questions with comparison operators.

```js
const maxGuests = 6;
const isOverCapacity = numGuests > maxGuests;
const hasEnoughSnacks = snacksOnHand >= totalSnacksNeeded;
```

Read the comparison aloud: “Is <em>numGuests greater than maxGuests?</em>” → the result becomes the boolean.

Common operators you’ll use here:

| Symbol | Reads like                         | Example                              |
| ------ | ---------------------------------- | ------------------------------------ |
| `>`    | greater than                       | `numGuests > maxGuests`              |
| `>=`   | greater than or equal to           | `snacksOnHand >= totalSnacksNeeded`  |
| `<`    | less than                          | `bowlsNeeded < 4`                    |
| `<=`   | less than or equal to              | `numGuests <= 10`                    |
| `===`  | strictly equal (same value & type) | `location === "living room"`        |
| `!==`  | strictly not equal                 | `snack !== "kettle corn"`           |

<Callout type="tip" title="Remember the assignment rule">
  <p className="m-0"><strong>`=`</strong> still means “evaluate the entire right-hand side first, then store the single resulting value in the variable on the left.” Comparisons live on the right-hand side.</p>
</Callout>

<HandbookChallenge
  title="Capacity alarms"
  hints={[
    "Re-use <code>numGuests</code>, <code>totalSnacksNeeded</code>, and <code>snacksOnHand</code> from Numbers.",
    "Introduce <code>const maxGuests = 6;</code> near your other numbers.",
    <pre key="peek" className="m-0 whitespace-pre-wrap">{`const maxGuests = 6;
let isOverCapacity = numGuests > maxGuests;
let hasEnoughSnacks = snacksOnHand >= totalSnacksNeeded;
console.log(isOverCapacity, hasEnoughSnacks);

numGuests += 2; // two more RSVPs
isOverCapacity = numGuests > maxGuests;
console.log(isOverCapacity);

snacksOnHand -= 3; // someone ate a bag early
hasEnoughSnacks = snacksOnHand >= totalSnacksNeeded;
console.log(hasEnoughSnacks);`}</pre>,
  ]}
  answers={
    <pre className="m-0 whitespace-pre-wrap">{`const maxGuests = 6;
let isOverCapacity = numGuests > maxGuests;
let hasEnoughSnacks = snacksOnHand >= totalSnacksNeeded;
console.log(isOverCapacity, hasEnoughSnacks);

numGuests += 2;
isOverCapacity = numGuests > maxGuests;
console.log(isOverCapacity);

snacksOnHand -= 3;
hasEnoughSnacks = snacksOnHand >= totalSnacksNeeded;
console.log(hasEnoughSnacks);`}</pre>
  }
>
  <p className="m-0">
    Add <code>maxGuests</code> to your planner. Compute <code>isOverCapacity</code> and <code>hasEnoughSnacks</code>, log them, then change the numbers (simulate new RSVPs or lost snacks) and recompute so the booleans stay accurate.
  </p>
  <p className="mt-3 mb-1 text-slate-300"><strong>Example console output:</strong></p>
  <pre className="m-0 whitespace-pre-wrap">{`false true
true
false`}</pre>
</HandbookChallenge>

---

## 3) Negation (`!`) turns one rule into its opposite

The exclamation mark means “not.” Use it to flip a boolean or to invert a whole condition.

```js
let isLightsDimmed = false;
isLightsDimmed = !isLightsDimmed; // true
const shouldSendReminder = !hasSentInvites; // true if invites aren’t sent yet
```

Every UI toggle you’ve ever built uses this pattern somewhere: <code>isOpen</code>, <code>isEditing</code>, <code>isMuted</code>, <code>isDarkMode</code>—they all flip with <code>!</code>.

<HandbookChallenge
  title="Lights & reminders"
  hints={[
    "Start with <code>let isLightsDimmed = false;</code> near your other booleans.",
    "Use <code>!variable</code> instead of manually writing true/false each time.",
    <pre key="peek" className="m-0 whitespace-pre-wrap">{`let isLightsDimmed = false;
console.log(isLightsDimmed); // false
isLightsDimmed = !isLightsDimmed;
console.log(isLightsDimmed); // true

const shouldSendReminder = !hasSentInvites;
console.log(shouldSendReminder);`}</pre>,
  ]}
  answers={
    <pre className="m-0 whitespace-pre-wrap">{`let isLightsDimmed = false;
console.log(isLightsDimmed);
isLightsDimmed = !isLightsDimmed;
console.log(isLightsDimmed);
isLightsDimmed = !isLightsDimmed;
console.log(isLightsDimmed);

const shouldSendReminder = !hasSentInvites;
console.log(shouldSendReminder);`}</pre>
  }
>
  <p className="m-0">
    Toggle <code>isLightsDimmed</code> twice with <code>!</code> to simulate testing the room ambiance, logging after each flip. Then create <code>shouldSendReminder</code> that’s <code>true</code> only when invites haven’t gone out yet (<code>!hasSentInvites</code>).
  </p>
  <p className="mt-3 mb-1 text-slate-300"><strong>Example console output:</strong></p>
  <pre className="m-0 whitespace-pre-wrap">{`false
true
false
false`}</pre>
</HandbookChallenge>

---

## 4) `null` vs `undefined`: empty on purpose vs empty by accident

Movie Night will eventually highlight a VIP friend or a featured snack, but maybe you haven’t chosen them yet. That’s where <code>null</code> comes in—it says “we meant for this to be empty right now.” <code>undefined</code> usually means “we never set this (or we looked in the wrong place).”

```js
let vipGuest = null;          // placeholder on purpose
let backupSnack;              // undefined because we never assigned
const snackPlan = { main: snack };
console.log(snackPlan.bonusDessert); // undefined (property missing)

vipGuest = "Aunt Jo";        // later we fill it in
console.log(vipGuest);        // "Aunt Jo"
```

<Callout type="tip" title="When to use which">
  <p className="m-0"><strong>Use <code>null</code></strong> when you want to signal “this will have a value later.” Expect <strong><code>undefined</code></strong> from JavaScript when you forget to assign or you ask for something that doesn’t exist.</p>
</Callout>

<HandbookChallenge
  title="Hold the VIP seat"
  hints={[
    "Set <code>let vipGuest = null;</code> near your booleans.",
    "Create an object like <code>const extras = { specialSnack: snack };</code> but skip another property.",
    <pre key="peek" className="m-0 whitespace-pre-wrap">{`let vipGuest = null;
console.log(vipGuest); // null
const extras = { specialSnack: snack };
console.log(extras.bonusDrink); // undefined
vipGuest = "Aunt Jo";
console.log(vipGuest);         // "Aunt Jo"
console.log(extras.bonusDrink); // still undefined`}</pre>,
  ]}
  answers={
    <pre className="m-0 whitespace-pre-wrap">{`let vipGuest = null;
console.log(vipGuest);
const extras = { specialSnack: snack };
console.log(extras.bonusDrink);
vipGuest = "Aunt Jo";
console.log(vipGuest);
console.log(extras.bonusDrink);`}</pre>
  }
>
  <p className="m-0">
    Initialize <code>vipGuest</code> to <code>null</code>, log it, then create a small <code>extras</code> object with one defined property and log a missing one to see <code>undefined</code>. Finally assign a real name to <code>vipGuest</code> and print both values again.
  </p>
  <p className="mt-3 mb-1 text-slate-300"><strong>Example console output:</strong></p>
  <pre className="m-0 whitespace-pre-wrap">{`null
undefined
Aunt Jo
undefined`}</pre>
</HandbookChallenge>

---

## Booleans checkpoint

<Callout type="tip" title="Full planner snapshot">
  <p className="m-0">By now, your script should include strings, numbers, and booleans working together. It might look like this (customize the values, but keep the structure so later chapters can build on it):</p>
  <pre className="m-0 whitespace-pre-wrap">{`let viewerName = "Milo";
let currentMood = "excited";
const location = "living room";
const streamingService = "CouchFlix";
const favoriteShow = "Mystery Manor";
let snack = "kettle corn";

const inviteLine = "Join " + viewerName + " in the " + location +
  " to stream " + favoriteShow + " with " + snack + ".";
const inviteMessage = \`Tonight, \${"{"}viewerName\} is streaming \${"{"}favoriteShow\}
  in the \${"{"}location\} with \${"{"}snack\} ready on \${"{"}streamingService\}.\`;

let numGuests = 3;
const maxGuests = 6;
const snacksPerGuest = 2;
const drinksPerGuest = 1;
let totalSnacksNeeded = numGuests * snacksPerGuest;
let totalDrinksNeeded = numGuests * drinksPerGuest;
let snacksOnHand = 10;
const snacksPerBowl = 4;
let bowlsNeeded = totalSnacksNeeded / snacksPerBowl;

let hasSentInvites = true;
let isProjectorOn = false;
const captionsEnabled = true;
let isLightsDimmed = false;
const isOverCapacity = numGuests > maxGuests;
const hasEnoughSnacks = snacksOnHand >= totalSnacksNeeded;
const shouldSendReminder = !hasSentInvites;
let vipGuest = "Aunt Jo";`}</pre>
  <p className="m-0">Use this as your reset point before diving into conditionals.</p>
</Callout>

---

## Common gotchas (mini road signs)

* Don’t put booleans in quotes—<code>"true"</code> is a string, not a boolean.
* <code>=</code> assigns, <code>===</code> compares, <code>!==</code> compares the opposite.
* Wrap the part you’re negating in parentheses when it improves readability: <code>const hasSpace = !(numGuests > maxGuests);</code>
* Use <code>null</code> to mark “waiting for real data later,” expect <code>undefined</code> when you forgot to assign or the property doesn’t exist.

---

## Technical Vocabulary

| Term                | Plain-English meaning                         | Tiny example                               |
| ------------------- | --------------------------------------------- | ------------------------------------------ |
| boolean             | A yes/no value                                | `const isReady = true`                     |
| comparison operator | Symbol that asks a question about numbers     | `numGuests > maxGuests`                    |
| assignment operator | Stores the result of an expression            | `isFull = seatsTaken === seatsTotal`       |
| negation operator   | Flips a boolean                               | `isLightsDimmed = !isLightsDimmed`         |
| null                | Empty on purpose (placeholder)                | `let vipGuest = null`                      |
| undefined           | Empty by accident / not set / missing property| `extras.bonusDrink // undefined`           |

---

## Wrap-up

* Booleans store the answers to the yes/no questions your app keeps asking.
* Derived booleans come from comparisons—ask the question on the right side of <code>=</code>.
* Use <code>!</code> to flip a value or invert a rule without rewriting the logic.
* Reach for <code>null</code> when you deliberately leave a slot empty, and expect <code>undefined</code> when something was never filled in.

**Next up: Conditionals.** We’ll feed these booleans into <code>if</code> / <code>else</code> statements so the planner can actually branch. The light switches you built here will finally control the flow.

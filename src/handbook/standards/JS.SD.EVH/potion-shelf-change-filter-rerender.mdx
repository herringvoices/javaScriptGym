---
id: JS.SD.EVH-06
title: Potion Shelf · Change → Filter → Re-render
short: Use the dropdown’s value to filter data and re-render the list.
---

# Change → Filter → Re-render

Last section, you got the dropdown working and proved your `change` handler fires by logging `event.target.value`.

Now you’re going to do the real version of that move:

**Use the selected value to change what’s rendered on the page.**

That’s the whole goal here. Not “state management.” Not “sync.” Not “architecture.”

Just the core interaction loop:

- user changes a control
- browser fires an event
- your handler reads the value
- your code renders the correct list

---

## The idea: filter the data first, then render the list

When beginners try to filter a list, the first impulse is usually:

> “I’ll hide some `<li>`s.”

That can work, but it’s not the clean move you’re practicing here.

The clean move is:

1. Decide which data should be visible
2. Render *only that* data into the list container

So your list is always “freshly built” from the data you chose.

---

## Canonical snippet: filter-driven render

Imagine a list of quests, each with a `difficultyId`, and a dropdown that selects difficulty.

```js
const renderQuestList = (difficultyId) => {
  const filtered = difficultyId === 0
    ? quests
    : quests.filter(q => q.difficultyId === difficultyId);

  document.querySelector("#questList").innerHTML =
    "<ul>" + filtered.map(q => `<li>${q.title}</li>`).join("") + "</ul>";
};
```

The render function chooses the data first, then builds the DOM from that chosen array.

---

## DOM values are strings

`event.target.value` comes from the DOM, so even numeric-looking option values arrive as strings:

- `"0"`, `"1"`, `"2"`, …

Strict comparisons care:

- `"2" === 2` is `false`

So convert the value before using it with numeric ids:

```js
const selectedId = parseInt(event.target.value);
```

---

<Callout type="tip" title="Parse once, then stop thinking about it">
  <p>Read from the DOM as a string, parse immediately, then use the parsed number everywhere else. That keeps the filtering logic predictable.</p>
</Callout>

---

<HandbookChallenge
  title="Filter the potion list by school"
  hints={[
    <>Put the list-rendering logic in a helper that accepts the selected school id; the handler should choose the id, not build the HTML itself.</>,
    <>Treat <code>0</code> as the “show everything” case. Any other id can create a filtered array with <code>filter()</code>.</>,
    <>Convert <code>event.target.value</code> to a number before calling the render helper, and keep <code>data-potion-id</code> on every rendered row so click behavior still works.</>,
  ]}
  expected={
    <p className="m-0"><strong>All Schools</strong> should show every potion. Selecting a school should immediately show only that school’s potions, and clicking any visible potion should still display its correct effect.</p>
  }
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`// /scripts/render.js
import { getPotions, getSchools } from "./database.js";

export const renderPotionList = (schoolId) => {
  const potions = getPotions();

  const filtered = schoolId === 0
    ? potions
    : potions.filter((p) => p.schoolId === schoolId);

  const items = filtered
    .map((p) => \`<li data-potion-id="\${p.id}">\${p.name}</li>\`)
    .join("");

  const potionListEl = document.querySelector("#potionList");
  potionListEl.innerHTML = \`<ul class="potionList">\${items}</ul>\`;
};

export const render = () => {
  // existing shell and school dropdown render stay here
  renderPotionList(0);
};

// /scripts/handlers.js
import { renderPotionList } from "./render.js";

export const bindEvents = () => {
  const schoolFilter = document.querySelector("#schoolFilter");

  schoolFilter.addEventListener("change", (event) => {
    const selectedId = parseInt(event.target.value);
    renderPotionList(selectedId);
  });
};`}</pre>
  }
>
  <p className="m-0">
    In <code>/scripts/render.js</code>, create and export <code>renderPotionList(schoolId)</code>. Get the potions, use all of them when <code>schoolId === 0</code>, otherwise filter by <code>p.schoolId</code>, then render the selected array into <code>#potionList</code> with the existing <code>data-potion-id</code> attributes. Call <code>renderPotionList(0)</code> during the initial render. In <code>/scripts/handlers.js</code>, import that helper, replace the proof-mode console log with a parsed <code>event.target.value</code>, and call the helper with the selected id. Test all-school, filtered, and potion-click behavior.
  </p>
  <Callout type="note" title="Keep this">
    <p className="m-0">Keep the change handler, immediate parsing of DOM values, filtering before rendering, and the reusable list renderer. Remove the old <code>console.log(event.target.value)</code> proof line.</p>
  </Callout>
</HandbookChallenge>

---

## Recap

You can now:

- Use a `change` event to drive real UI updates
- Parse DOM string values into numbers for reliable comparisons
- Filter an array based on a selected id
- Re-render a list from filtered data instead of manually hiding DOM nodes
- Combine event handling and rendering into a clean, repeatable loop

That’s the heart of interactive UI: **listen → read → decide → render.**

---

## Technical Vocabulary

| Term           | Meaning                                                                 |
| -------------- | ----------------------------------------------------------------------- |
| `filter()`     | Creates a new array containing only items that match a condition.       |
| parse          | Converting a string into a number so it can be compared mathematically. |
| re-render      | Updating what’s on screen by running your render logic again.           |
| `change` event | Fires when a form control’s value changes (like a dropdown).            |
| argument       | A value passed into a function so it can behave differently.            |

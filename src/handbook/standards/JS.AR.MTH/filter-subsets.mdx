---
id: JS.AR.MTH.FILTER
title: Array Methods · filter() Creates Subsets
short: Use filter() to keep only the students who match a house rule.
---
# Array Methods → Filtering Arrays

## The `filter()` Method

`filter()` does exactly what it says on the tin: it lets you filter an array, and keep only the items that match a rule. It runs your callback on each item and:

- keeps it if the callback returns `true`
- skips it if the callback returns `false`

This callback is often called a **predicate**: it answers “yes or no?” for each item.

**Example:**

```js
const petOwners = students.filter((s) => s.pets > 0);
```

Now `petOwners` contains only student objects with at least one pet.

What about an example from real life? Maybe you've decided to surround yourself with only beautiful people?
```js
const myFriends = people.filter(p => !p.isUgly)
```
There! Now the `myFriends` array is only full of people for whom `isUgly` is `false` (note the `!`).

Maybe you love Starbursts, but only the pink ones?

```js
const pinkStarbursts = starbursts.filter((s) => s.color === "pink");
```
We could do this all day! The point is, `filter()` lets you pick through an array **item by item** and decide what should **_stay_** and what should **_go_** based on a condition.

---

### House sorting rules

At Boarblemish, students are sorted into houses based on their `nature`:

- `"brave"` → Dragonport
- `"smart"` → Crowbeak
- `"sneaky"` → Sidleup
- everything else → Heavysigh

You’ll use `filter()` to create a separate array for each house.

<HandbookChallenge
  title="Sort the houses with a loop"
  hints={[
    <>Each student should be pushed into exactly one house array.</>,
    <>One <code>if / else if / else</code> chain prevents a student from being added to multiple branches.</>,
    <>The final <code>else</code> can collect every nature that did not match the three named houses.</>,
  ]}
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`const dragonportHouse = [];
const crowbeakHouse = [];
const sidleupHouse = [];
const heavysighHouse = [];

for (const s of students) {
  if (s.nature === "brave") {
    dragonportHouse.push(s);
  } else if (s.nature === "smart") {
    crowbeakHouse.push(s);
  } else if (s.nature === "sneaky") {
    sidleupHouse.push(s);
  } else {
    heavysighHouse.push(s);
  }
}

console.log(
  dragonportHouse.length,
  crowbeakHouse.length,
  sidleupHouse.length,
  heavysighHouse.length
);`}</pre>
  }
>
  <ul className="m-0 list-disc space-y-1 pl-5">
    <li>Create empty arrays named <code>dragonportHouse</code>, <code>crowbeakHouse</code>, <code>sidleupHouse</code>, and <code>heavysighHouse</code>.</li>
    <li>Loop through <code>students</code> with <code>for...of</code>.</li>
    <li>Sort <code>"brave"</code> students into Dragonport, <code>"smart"</code> into Crowbeak, <code>"sneaky"</code> into Sidleup, and everything else into Heavysigh.</li>
    <li>Log the length of each house array to check the result.</li>
    <li>Keep this only as a comparison version; once the <code>filter()</code> version works, you can remove the loop.</li>
  </ul>
</HandbookChallenge>

<HandbookChallenge
  title="Sort the houses with filter()"
  hints={[
    <><code>filter()</code> creates a new array containing only items whose callback returns <code>true</code>.</>,
    <>Each named nature can use its own small comparison callback.</>,
    <>For Heavysigh, a helper predicate can return true only when none of the three named natures match.</>,
  ]}
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`const dragonportHouse = students.filter((s) => s.nature === "brave");
const crowbeakHouse = students.filter((s) => s.nature === "smart");
const sidleupHouse = students.filter((s) => s.nature === "sneaky");

const heavysighHouseCheck = (student) => {
  return (
    student.nature !== "brave" &&
    student.nature !== "smart" &&
    student.nature !== "sneaky"
  );
};

const heavysighHouse = students.filter(heavysighHouseCheck);

console.log(
  dragonportHouse.length,
  crowbeakHouse.length,
  sidleupHouse.length,
  heavysighHouse.length
);`}</pre>
  }
>
  <ul className="m-0 list-disc space-y-1 pl-5">
    <li>Create <code>dragonportHouse</code>, <code>crowbeakHouse</code>, and <code>sidleupHouse</code> with <code>filter()</code> using the three nature rules.</li>
    <li>Create <code>heavysighHouseCheck(student)</code> so it returns <code>true</code> only when the nature is not <code>"brave"</code>, <code>"smart"</code>, or <code>"sneaky"</code>.</li>
    <li>Create <code>heavysighHouse</code> by passing that helper to <code>students.filter()</code>.</li>
    <li>Log the four array lengths and compare them with the loop version.</li>
    <li>Keep the filtered house arrays and Heavysigh helper for later chapters.</li>
  </ul>
</HandbookChallenge>

You should now have `dragonportHouse`, `crowbeakHouse`, `sidleupHouse`, `heavysighHouse`, and a `heavysighHouseCheck(student)` helper.

---
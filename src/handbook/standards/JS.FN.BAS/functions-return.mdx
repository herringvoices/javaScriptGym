---

id: JS.FN.BAS.RETURN
title: Functions · Return Values (Getting Results Back)
short: Use return to hand values back from functions so you can store and reuse results.
---

# Functions → Return Values (Getting Results Back)

So far, your functions have mostly **logged** things.

* `greet(name)` logs a greeting.
* `announceSection(section)` logs a message.
* `logWith(tag, message)` logs a tagged line.

That’s great for humans reading the console, but sometimes your code doesn’t just want to **see** a value — it wants to **use** it.

You don’t just want the chef to yell, “Food is ready!”.
You want them to **hand you the plate**.

That’s what **`return`** is for.

---

## 1) The problem: logging isn’t enough

Imagine you want to show a customer’s full name in a couple of different places:

```js
const fullName = (first, last) => {
  console.log(first + " " + last);
};

fullName("Ada", "Lovelace");
// Later...
const label = "Customer: " + ???;
```

You can log the name, sure, but when you go to build `label`, there’s nothing to plug in. The function shouted the answer into the console and then it vanished.

Your code wants:

```js
const label = "Customer: " + (the full name);
```

It needs the **value**, not just a log.

That’s where `return` comes in: it lets your function **hand a value back** to whoever called it.

---

## 2) What is a return? (Chef hands you the plate)

Calling a function is like shouting an order to the kitchen:

```js
const result = makeDish();
```

* The function runs its steps.
* At some point, it hits a **`return`**.
* Whatever you `return` becomes the **result** of the function call — the plate the chef hands back.

In JavaScript:

```js
const fullName = (first, last) => {
  const combined = first + " " + last;
  return combined; // hand this value back
};

const name = fullName("Ada", "Lovelace");
console.log(name); // "Ada Lovelace"
```

<HandbookChallenge
title="F1-5 · Create users"
hints={[
"Write a `const createUser = (first, last) => { ... }` function that builds and returns an object with `firstName` and `lastName` properties.",
"Invoke `createUser` three times, store each returned object in a variable, and `console.log` those variables.",
<pre key="peek" className="m-0 whitespace-pre-wrap">{`const createUser = (first, last) => {
  const newUser = {
    firstName: first,
    lastName: last,
  };
  return newUser;
};

const u1 = createUser("Ada", "Lovelace");
console.log(u1);`}</pre>,
]}
answers={ <pre className="m-0 whitespace-pre-wrap">{`const createUser = (first, last) => {
const newUser = {
  firstName: first,
  lastName: last,
}
return newUser
};

const userA = createUser("Ada", "Lovelace");
const userB = createUser("Alan", "Turing");
const userC = createUser("Grace", "Hopper");
console.log(userA, userB, userC);`}</pre> }
>
  <p className="m-0">
    Create a `createUser(first, last)` function that returns a new user object with `firstName` and `lastName` properties. Call it three times, store each result in a variable, and `console.log` those variables to inspect the objects.
  </p>
</HandbookChallenge>

---

<HandbookChallenge
title="F1-6 · Messages array and createMessage"
hints={[
  "Declare an array named `messages` and a function `createMessage(msg, userObj)` that returns an object `{ message: msg, user: userObj }`.",
  "Create several message objects by calling `createMessage` and store them in variables, then `messages.push(...)` to add them to the array.",
  <pre key="peek" className="m-0 whitespace-pre-wrap">{`const messages = [];
const createMessage = (msg, userObj) => {
  return { message: msg, user: userObj };
};

const m1 = createMessage("Hello!", userA);
messages.push(m1);`}</pre>,
]}
answers={ <pre className="m-0 whitespace-pre-wrap">{`const messages = [];
const createMessage = (msg, userObj) => {
  return { message: msg, user: userObj };
};

const msg1 = createMessage("Welcome!", userA);
const msg2 = createMessage("Nice to meet you", userB);
const msg3 = createMessage("Goodbye!", userC);
messages.push(msg1, msg2, msg3);
console.log(messages);`}</pre> }
>
  <p className="m-0">
    Create a `messages` array and a `createMessage(msg, userObj)` helper that returns an object with the message text and the user who wrote it. Use the helper to make several message objects, store them in variables, and push them onto the `messages` array.
  </p>
</HandbookChallenge>

---

<HandbookChallenge
title="F1-7 · Welcome that uses user and messages"
hints={[
  "Change `welcome` so it accepts two parameters: a `user` object and the `messages` array.",
  "Use dot notation to access `user.firstName` and `user.lastName`, and `messages.length` for the unread count.",
  <pre key="peek" className="m-0 whitespace-pre-wrap">{`const welcome = (user, messages) => {
  console.log(\`=== Welcome, \${user.firstName} \${user.lastName}! ===\`)
  console.log(\`You have \${messages.length} unread messages!\`)
};

welcome(userA, messages);
welcome(userB, []); // try different arrays`}</pre>,
]}
answers={ <pre className="m-0 whitespace-pre-wrap">{`const welcome = (user, messages) => {
const full = \`=== Welcome, \${user.firstName} \${user.lastName}! ===\`;
const count = \`You have \${messages.length} unread messages!\`;
console.log(full);
console.log(count);
};

welcome(userA, messages);
welcome(userB, []);`}</pre> }
>
  <p className="m-0">
    Update `welcome` to accept a `user` object and a `messages` array. Inside, use dot notation (for example `user.firstName`) to show the user's name and use `messages.length` to show how many messages they have. Call the function with different users and add comments to your code to explain what is happening line-by-line from line 1 to the last.
  </p>
</HandbookChallenge>

---

## Wrap-up

* **`return`** is how a function hands a value back to whoever called it.
* Whatever you return **takes the place of the function call** in the code.
* You can store that result in a variable, or use it directly inside bigger expressions.
* Once a function hits `return`, it stops running anything after that line.

Next up, you’ll see how to make small functions that **call each other**, so you can build bigger behavior out of tiny, focused helpers.

---

id: JS.VDT.COL.ARR_OBJ
title: Arrays of Objects · Playlists of Records
short: Store rows of related fields as objects inside an ordered array; access fields by index and dot paths, update them, and add a new record at the end by index.
---

# Data Types → Arrays of Objects

*Last time we* used  **objects (binders)** to group labeled fields and **arrays (playlists)** to keep values in order. *This time we’re* combining those ideas: an **array of objects**—an ordered list where **each item is a small object** that keeps its fields together. It reads like real data: a playlist where every track carries both a `title` and an `artist` in one piece.

---

## When you need more than a title

Suppose you want to represent both the **title** and the **artist** for each song. A first thought might be two arrays—one for titles and one for artists—then read them at the same index.

```js
const titles  = ["Sunset Drive", "Neon Rain", "Echoes & Honey", "Midnight Paper Planes"];
const artists = ["Axton",        "Riza",       "Palmer",        "Paper Planes Collective"];

console.log(titles[2], "by", artists[2]); // "Echoes & Honey by Palmer"
```

This only works if both arrays stay perfectly aligned. Insert, remove, or reorder in one array and the pairing breaks. You’re left babysitting two lists that want to wander off in different directions.

---

## The better fit: one ordered list of full records

Arrays can hold **any** data type, including objects. So make **one array** where **each item is an object** with the fields that belong together.

```js
const songs = [
  { title: "Sunset Drive",          artist: "Axton" },
  { title: "Neon Rain",             artist: "Riza" },
  { title: "Echoes & Honey",        artist: "Palmer" },
  { title: "Midnight Paper Planes", artist: "Paper Planes Collective" }
];
console.log(songs);
```

Each object is a single record; the array keeps their order.

---

id: JS.VDT.COL.ARR_OBJ
title: Arrays of Objects · Shelves of Books
short: Store full book records as objects inside an ordered array; access and update them by index and dot path.
---

# Data Types → Arrays of Objects (Book Records)

You’ve seen **book objects** (one binder per book) and **arrays of primitives** (titles in order). Now we’ll combine those ideas: an **array of book objects**—an ordered shelf where each slot holds a full book record.

---

## Before you start: make sure Checkpoint C is loaded

This chapter assumes you already have <code>book1</code> and <code>book2</code> objects, and probably a <code>titles</code> array. At minimum, you need:

```js
const book1 = {
  title: "The Way of Kings",
  author: "Brandon Sanderson",
  pages: 1010,
  isPaperback: false,
  genre: "Fantasy",
};

const book2 = {
  title: "Words of Radiance",
  author: "Brandon Sanderson",
  pages: 1087,
  isPaperback: false,
};
```

If your editor doesn’t have <code>book1</code> and <code>book2</code> objects like this, clear it and paste the **Checkpoint B or C** snippet from the previous chapters.

---

## 1) Add a third book object

First, create a new book: <code>book3</code>.

```js
const book3 = {
  title: "Oathbringer",
  author: "Brandon Sanderson",
  pages: 1248,
  isPaperback: false,
};
```

<HandbookChallenge
title="Challenge D1: Create `book3`"
hints={[
  "Follow the same shape as book1 and book2.",
  "Include at least title, author, pages, and isPaperback.",
  <pre key="peek" className="m-0 whitespace-pre-wrap">{`const book3 = {
  title: "Oathbringer",
  author: "Brandon Sanderson",
  pages: 1248,
  isPaperback: false,
};

console.log(book3.title);`}</pre>,
]}
answers={<pre className="m-0 whitespace-pre-wrap">{`const book3 = {
  title: "Oathbringer",
  author: "Brandon Sanderson",
  pages: 1248,
  isPaperback: false,
};

console.log(book3.title); // Oathbringer`}</pre>}
>
  <p className="m-0">
    Define a <code>book3</code> object with the same fields as your earlier books, then log its title to confirm.
    </p>
</HandbookChallenge>

---

## 2) Assemble a `books` shelf (array of book objects)

Now combine <code>book1</code>, <code>book2</code>, and <code>book3</code> into a single ordered shelf.

```js
const books = [book1, book2, book3];
console.log(books.length);      // 3
console.log(books[0].title);    // first book’s title
console.log(books[2].author);   // third book’s author
```

<HandbookChallenge
title="Challenge D2: Assemble `books` array"
hints={[
  "Create a constant named books that holds [book1, book2, book3].",
  "Use books.length to log how many books are on the shelf.",
  "Use books[0].title and books[2].author to read specific fields.",
]}
answers={<pre className="m-0 whitespace-pre-wrap">{`const books = [book1, book2, book3];

console.log(books.length);    // 3
console.log(books[0].title);  // The Way of Kings
console.log(books[2].author); // Brandon Sanderson`}</pre>}
>
  <p className="m-0">
    Build a <code>books</code> array containing your three book objects, then practice reading its length and a couple of fields using index + dot notation.
    </p>
</HandbookChallenge>

> If an index is out of range, <code>books[index]</code> is <code>undefined</code>. If a key is misspelled, <code>books[1].pagse</code> also gives <code>undefined</code>. Keep both indexes and property names precise.

---

## 3) Update one record via the array

You don’t have to reach for the original variable name (<code>book2</code>) to update a book—you can go through the array.

```js
console.log(books[1].pages); // original pages
books[1].pages = 1100;       // updated edition
console.log(books[1]);       // shows updated pages
```

<HandbookChallenge
title="Challenge D3: Update one record through `books`"
hints={[
  "Start by logging books[1].pages to see the current count.",
  "Assign a new number to books[1].pages to simulate an updated edition.",
  "Log books[1] again to see the full updated object.",
]}
answers={<pre className="m-0 whitespace-pre-wrap">{`console.log(books[1].pages);
books[1].pages = 1100;
console.log(books[1]);
// { title: "Words of Radiance", author: "Brandon Sanderson", pages: 1100, isPaperback: false }`}</pre>}
>
  <p className="m-0">
    Use the <code>books</code> array plus dot notation to fix the page count for the second book. Logging before and after makes the change obvious.
    </p>
</HandbookChallenge>

> <code>const books = [book1, book2, book3]</code> keeps the variable’s link to the array stable. Editing a field inside one of the objects is normal and allowed.

---

## Checkpoint D · Three book objects on a shelf

By the end of this chapter, your editor should look roughly like this (values can vary slightly, but keep the structure):

```js
const book1 = {
  title: "The Way of Kings",
  author: "Brandon Sanderson",
  pages: 1010,
  isPaperback: false,
  genre: "Fantasy",
};

const book2 = {
  title: "Words of Radiance",
  author: "Brandon Sanderson",
  pages: 1100,
  isPaperback: false,
};

const book3 = {
  title: "Oathbringer",
  author: "Brandon Sanderson",
  pages: 1248,
  isPaperback: false,
};

const books = [book1, book2, book3];

console.log(books.length);
console.log(books[0].title);
console.log(books[2].author);
```

You’ll use this <code>books</code> array to practice adding and removing books from the ends of the shelf in the next chapter.

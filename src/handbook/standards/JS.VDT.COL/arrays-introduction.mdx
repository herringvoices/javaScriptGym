---

id: JS.VDT.COL.ARR
title: Arrays · Lists of Book Data
short: Group related book fields into ordered lists, read by index, and update specific spots.
---

# Data Types → Arrays (Book Titles & Counts)

Last time, you turned <code>book1Title</code> and friends into <code>book1</code> and <code>book2</code> objects. Now we’ll build **arrays of primitives** on top of those objects—ordered lists of titles and page counts.

---

## Before you start: make sure Checkpoint B is loaded

This chapter assumes your editor already has <code>book1</code> and <code>book2</code> objects, like this:

```js
const book1 = {
  title: "The Way of Kings",
  author: "Brandon Sanderson",
  pages: 1010,
  isPaperback: false,
  genre: "Fantasy",
};

const book2 = {
  title: "Words of Radiance",
  author: "Brandon Sanderson",
  pages: 1087,
  isPaperback: false,
};
```

If your editor does not have <code>book1</code> and <code>book2</code> objects like this, clear it and paste the **Checkpoint B** snippet from the Objects chapter.

---

## 1) A list of titles in one variable

Arrays are like **numbered shelves**: a single variable that holds many values **in order**.

```js
const titles = [book1.title, book2.title, "Oathbringer"];
console.log(titles);
```

<HandbookChallenge
title="Challenge C1: Titles array"
hints={[
"Create a constant named titles that holds three strings: book1.title, book2.title, and a third literal like 'Oathbringer'.",
"Wrap the values in square brackets [ ... ] with commas between them.",
<pre key="peek" className="m-0 whitespace-pre-wrap">{`const titles = [book1.title, book2.title, "Oathbringer"];
console.log(titles);`}</pre>,
]}
answers={ <pre className="m-0 whitespace-pre-wrap">{`const titles = [book1.title, book2.title, "Oathbringer"];
console.log(titles);
// ["The Way of Kings","Words of Radiance","Oathbringer"]`}</pre>
}
>
  <p className="m-0">
    Build a <code>titles</code> array from your existing book objects plus one extra book, then log the whole array to see all titles at once.
  </p>
  <p className="mt-3 mb-1 text-slate-300"><strong>Expected console output (example):</strong></p>
  <pre className="m-0 whitespace-pre-wrap">{`["The Way of Kings","Words of Radiance","Oathbringer"]`}</pre>
</HandbookChallenge>

---

## 2) Read by index (positions start at 0)

Each item in an array has a position called an **index**. Arrays are **zero-based**: the first item is at index <code>0</code>, the second at <code>1</code>, and so on.

```js
console.log(titles[0]);                 // first → "The Way of Kings"
console.log(titles[titles.length - 1]); // last  → "Oathbringer"
```

<HandbookChallenge
title="Challenge C2: Index and log"
hints={[
"Use titles[0] to log the first title.",
"Use titles[titles.length - 1] to log the last title.",
"Two separate console.log calls: one for the first, one for the last.",
]}
answers={ <pre className="m-0 whitespace-pre-wrap">{`console.log(titles[0]);                 // "The Way of Kings"
console.log(titles[titles.length - 1]); // "Oathbringer"`}</pre>
}
>
  <p className="m-0">
    Log the first and last titles from <code>titles</code> using bracket notation with indexes.
  </p>
  <p className="mt-3 mb-1 text-slate-300"><strong>Expected console output (example):</strong></p>
  <pre className="m-0 whitespace-pre-wrap">{`The Way of Kings
Oathbringer`}</pre>
</HandbookChallenge>

> <strong>Past the edge</strong><br />
> If an index doesn’t exist (too big or negative), the result is <code>undefined</code>. That’s JavaScript’s way of saying “no value lives here.”

---

## 3) Change a specific position

You can replace a single item by assigning to that index. The variable still points to the same array—only one slot inside changes.

```js
console.log(titles[1]);                  // "Words of Radiance"
titles[1] = "Words of Radiance (Revised Edition)";
console.log(titles[1]);                  // updated value
```

<HandbookChallenge
title="Challenge C3: Update an element"
hints={[
"Start by logging titles[1] to see the original second title.",
"Assign a new string to titles[1] to simulate an updated edition.",
"Log the whole titles array again to see the change in context.",
]}
answers={ <pre className="m-0 whitespace-pre-wrap">{`console.log(titles[1]);
titles[1] = "Words of Radiance (Revised Edition)";
console.log(titles[1]);
console.log(titles);
// ["The Way of Kings","Words of Radiance (Revised Edition)","Oathbringer"]`}</pre>
}
>
  <p className="m-0">
    Log the second title, update that position with a revised name, then log again to confirm the change.
  </p>
  <p className="mt-3 mb-1 text-slate-300"><strong>Expected console output (example):</strong></p>
  <pre className="m-0 whitespace-pre-wrap">{`Words of Radiance
Words of Radiance (Revised Edition)
["The Way of Kings","Words of Radiance (Revised Edition)","Oathbringer"]`}</pre>
</HandbookChallenge>

---

## Optional: page counts as a parallel list

If you’d like a second array to mirror <code>titles</code>, you can create a <code>pageCounts</code> array using numbers.

```js
const pageCounts = [book1.pages, book2.pages, 1248];
console.log(pageCounts);
```

This mirrors <code>titles</code> by index but uses numbers instead of strings.

---

## Checkpoint C · Titles (and maybe page counts)

By the end of this chapter, your editor should look roughly like this (values can differ, but keep the structure):

```js
const book1 = {
  title: "The Way of Kings",
  author: "Brandon Sanderson",
  pages: 1010,
  isPaperback: false,
  genre: "Fantasy",
};

const book2 = {
  title: "Words of Radiance",
  author: "Brandon Sanderson",
  pages: 1087,
  isPaperback: false,
};

const titles = [
  book1.title,
  book2.title,
  "Oathbringer",
];

titles[1] = "Words of Radiance (Revised Edition)";

console.log(titles);
// ["The Way of Kings","Words of Radiance (Revised Edition)","Oathbringer"]
```

You’ll use <code>book1</code>, <code>book2</code>, and these ideas to build a full <code>books</code> array of objects in the next chapter.

---
id: JS.PF.ITR
title: Iteration · Repeating Actions on Purpose
short: Use for and for…of loops to walk collections, do work for each item, and avoid common loop mistakes.
---

# Iteration → Repeating Actions on Purpose

Welcome! In this chapter you’ll meet another big idea in JavaScript: **iteration** — the way your program repeats a set of actions. Most of the time, that means doing the same kind of work for **each item in an array**.

Now, that might not sound particularly useful… until you need to:

* **total up** everything in a customer’s cart
* **scan** a library’s checked-out books to see whether any are overdue
* **use** data from a database to build a dropdown menu (one option per item)

Notice what those have in common: you’re not dealing with one thing. You’re dealing with a **collection**—a list of cart items, a stack of books, a batch of records. To do anything useful, your code has to move through that collection one item at a time and do the same kind of work each time.

That’s where **loops** come in. Loops are how you tell JavaScript: “Take this set of instructions… and repeat it for each item until we’re done.”

Think of it like a machine that runs the same script over and over.
Set it up to make tacos, and it makes taco after taco until you shut it off.
Set it up to email your grandmother, and—well—she’s going to have questions.

Iteration is that idea in code: a repeatable process, driven by a clear rule that eventually says, “Alright. Stop.”

---

## Practice · Spot repetition before loops

Before we let JavaScript act as the repeating machine for us, you’ll do things the “manual” way once. For now, **you** are going to be the machine: you’ll repeat the same pattern by hand and feel how repetitive it is.

The examples below assume you already have a `heroes` array in your file with several hero objects. You’ll work with that data directly.

<HandbookChallenge
  title="Welcome every hero by hand"
  hints={[
    <>Bracket access lets you choose one hero by position before reading its <code>name</code>.</>,
    <>Get the message working for one hero first, then repeat the same pattern for each remaining index.</>,
    <>When the array grows, every manual line and the manually stored count must grow with it.</>,
  ]}
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`// Manual welcomes for the original heroes
console.log(heroes[0].name + " has arrived!");
console.log(heroes[1].name + " has arrived!");
console.log(heroes[2].name + " has arrived!");

const heroCount = 3;
console.log("There are " + heroCount + " heroes in the League.");

heroes.push({ id: 4, name: "Aurora Pulse" });

// The manual version now needs another line and another count.
console.log(heroes[0].name + " has arrived!");
console.log(heroes[1].name + " has arrived!");
console.log(heroes[2].name + " has arrived!");
console.log(heroes[3].name + " has arrived!");

const updatedHeroCount = 4;
console.log("There are " + updatedHeroCount + " heroes in the League.");`}</pre>
  }
>
  <p className="m-0">
    Using your existing <code>heroes</code> array, write one <code>console.log()</code> per hero that prints a message like <code>"Crimson Phoenix has arrived!"</code> by accessing each hero by index. Declare <code>heroCount</code> and log the number of heroes. Then push one new hero into the array and manually update both the welcome lines and the count so everything is correct again.
  </p>
</HandbookChallenge>

<HandbookChallenge
  title="Build the hero names array by hand"
  hints={[
    <>Start with an empty array that will collect only the names.</>,
    <>For each position in <code>heroes</code>, read that hero’s <code>name</code> and add the value to the new array.</>,
    <><code>push()</code> adds one value to the end of an array.</>,
  ]}
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`const heroNames = [];

heroNames.push(heroes[0].name);
heroNames.push(heroes[1].name);
heroNames.push(heroes[2].name);
heroNames.push(heroes[3].name);

console.log(heroNames);`}</pre>
  }
>
  <p className="m-0">
    Create an empty array named <code>heroNames</code>. Without using a loop, push each hero’s <code>name</code> into it one at a time with bracket access, then log <code>heroNames</code>.
  </p>
</HandbookChallenge>

---

You just acted like a very patient, very manual loop:

* You **started** with the first hero.
* You **stopped** when you ran out of heroes.
* You **stepped** forward by changing the index or picking the next hero yourself.

In the next chapters, we’ll hand this job to actual JavaScript loops so you don’t have to be the machine anymore.

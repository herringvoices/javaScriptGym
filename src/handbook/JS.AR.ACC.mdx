---
id: JS.AR.ACC
title: Arrays & Objects · Mutation and the "Wait… Why Did That Change?!" Bug
short: Arrays and objects are reference types, so they can change in surprising ways—learn how mutation works and fix accidental shared data with spread (...).
---

# Arrays & Objects → Mutation and the "Wait… Why Did That Change?!" Bug

So far, arrays have mostly behaved the way you expected: you add things, remove things, loop through them, life is good.

But arrays (and objects) have a secret: they don’t work like primitives.

When you assign a **primitive** (like a number or a string), you get an actual copy of the value:

```js
let a = 5;
let b = a;
b = 99;

console.log(a); // 5
console.log(b); // 99
```

Changing `b` didn’t change `a`, because `b` got its own separate value.

But **arrays and objects are reference types**. When you assign one, you’re not copying the whole array/object — you’re copying a *reference* to it (basically: “here’s where that thing lives”).

So this can happen:

```js
const nums = [1, 2, 3];
const otherNums = nums;

otherNums.pop();

console.log(nums);      // [1, 2]
console.log(otherNums); // [1, 2]
```

That’s the "Wait… why did that change?!" bug.

In this one-page chapter, you’ll intentionally create that bug twice, then learn the everyday fix: using the **spread operator** to make shallow copies before you mutate.

---

## 1) The production line problem

You’ve got several **car blueprints** arriving at a factory. Your job is to add them to the production line with a new id.

Your starter code includes:

* `let nextId = 1`
* `const productionLine = []`
* a blueprint object like `sedanBlueprint`

### First, do it the “obvious” way

<HandbookChallenge
  title="Reuse one blueprint on the production line"
  hints={[
    <>Objects are reference types, so passing the same blueprint into a function passes access to the same object each time.</>,
    <>Assigning properties directly to the <code>car</code> parameter changes the object that was passed in.</>,
    <>When the same object is pushed into an array more than once, each entry can point to that same object.</>,
  ]}
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`const addCar = (car, color) => {
  car.id = nextId;
  nextId++;
  car.color = color;
  productionLine.push(car);
};

addCar(sedanBlueprint, "red");
addCar(sedanBlueprint, "blue");
addCar(sedanBlueprint, "green");

console.log(productionLine);`}</pre>
  }
>
  <ul className="m-0 list-disc space-y-1 pl-5">
    <li>Write <code>addCar(car, color)</code>.</li>
    <li>Inside it, assign <code>car.id = nextId</code>, increment <code>nextId</code>, set <code>car.color</code> from the parameter, and push <code>car</code> into <code>productionLine</code>.</li>
    <li>Call <code>addCar</code> three times with the same <code>sedanBlueprint</code> object but different colors.</li>
    <li>Log <code>productionLine</code> and inspect the objects. What do you notice about their IDs and colors?</li>
  </ul>
</HandbookChallenge>

### What’s going on?

When you do this:

```js
productionLine.push(car)
```

…you’re not putting a fresh copy into the array. You’re putting a **reference** to an object.

So if you reuse the same blueprint object, you’re basically doing:

* “Put this blueprint on the line.”
* “Put this same blueprint on the line again.”
* “Put this same blueprint on the line again again.”

Now you’ve got multiple array entries pointing at the **same object**. If you mutate that object (like setting `car.id`), every reference “sees” the latest mutation.

---

## 2) Mutation vs non-mutation

**Mutating** means: you change the original thing in place.

```js
car.id = 7;            // mutates the original object
productionLine.push(); // mutates the original array
productionLine.pop();  // mutates the original array
```

**Non-mutating** means: you don’t change the original; you create/return something new.

In a lot of real apps, you’ll still mutate arrays and objects. The goal isn’t “never mutate.” The goal is:

> Don’t mutate something you didn’t mean to share.

So you need a quick way to make a new array or object before you start changing it.

---

## 3) The fix for objects: the spread operator

The **spread operator** is `...`.

When you use it inside an object, it creates a **shallow copy**:

```js
const newCar = { ...car, id: nextId };
```

That says:

* “Make a new object with all of `car`’s properties…”
* “…and also set/override `id`.”

Now you’re not mutating the blueprint. You’re creating a new car.

<HandbookChallenge
  title="Copy the blueprint before adding a car"
  hints={[
    <>Spread inside an object literal creates a new outer object with the original object’s properties.</>,
    <>Put properties after <code>...car</code> when you want those copied values to be overridden.</>,
    <>Push the newly created object, not the original blueprint, into <code>productionLine</code>.</>,
  ]}
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`const addCar = (car) => {
  const newCar = { ...car, id: nextId };
  nextId++;
  productionLine.push(newCar);
};

addCar(hondordGrove);
addCar(hondordGrove);
addCar(hondordGrove);

console.log(productionLine);`}</pre>
  }
>
  <ul className="m-0 list-disc space-y-1 pl-5">
    <li>Refactor <code>addCar(car)</code> so it does not change the original blueprint.</li>
    <li>Create <code>newCar</code> with object spread and the current <code>nextId</code>.</li>
    <li>Increment <code>nextId</code>, then push <code>newCar</code> into <code>productionLine</code>.</li>
    <li>Call the function three times with the same Hondord blueprint object, such as <code>hondordGrove</code>.</li>
    <li>Log <code>productionLine</code> and compare its records to the first attempt.</li>
  </ul>
</HandbookChallenge>

---

## 4) The dealership problem

Now you need to send inventory to three dealership branches:

* west
* east
* central

You decide to make three “inventories” from the production line.

### First, do it the “obvious” way

<HandbookChallenge
  title="Share one inventory across three branches"
  hints={[
    <>Assigning an array to another variable copies the reference, not the array itself.</>,
    <>If three variables all point to <code>productionLine</code>, a mutation through any one of them reaches the same array.</>,
    <><code>pop()</code> mutates the array it is called on.</>,
  ]}
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`const westBranchInventory = productionLine;
const eastBranchInventory = productionLine;
const centralBranchInventory = productionLine;

const eastShowroomCar = eastBranchInventory.pop();

console.log("West inventory:", westBranchInventory);
console.log("East inventory:", eastBranchInventory);
console.log("Central inventory:", centralBranchInventory);`}</pre>
  }
>
  <ul className="m-0 list-disc space-y-1 pl-5">
    <li>Create <code>westBranchInventory</code>, <code>eastBranchInventory</code>, and <code>centralBranchInventory</code>, assigning each one directly to <code>productionLine</code>.</li>
    <li>Call <code>pop()</code> on <code>eastBranchInventory</code> and store the removed car in <code>eastShowroomCar</code>.</li>
    <li>Log all three branch inventories.</li>
    <li>Explain why changing only the east branch appears to change the other two branches too.</li>
  </ul>
</HandbookChallenge>

### What’s going on?

This line:

```js
const eastBranchInventory = productionLine;
```

does **not** make a copy.

It makes a new variable that points at the **same array**.

So when one branch does `pop()`, it mutates the shared array — which means everyone’s “inventory” changes, because it’s all one inventory wearing three different name tags.

---

## 5) The fix for arrays: spread-copy before you mutate

Spread works for arrays too:

```js
const eastBranchInventory = [...productionLine];
```

That creates a new array with the same items.

Now each branch can mutate its own array without accidentally stealing from the other branches.

<HandbookChallenge
  title="Give each branch its own inventory array"
  hints={[
    <>Spread inside an array literal creates a new outer array containing the same items.</>,
    <>Create each branch inventory separately instead of assigning the shared <code>productionLine</code> reference.</>,
    <>After copying, mutating one branch’s outer array should not change the other branch arrays.</>,
  ]}
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`const westBranchInventory = [...productionLine];
const eastBranchInventory = [...productionLine];
const centralBranchInventory = [...productionLine];

const eastShowroomCar = eastBranchInventory.pop();

console.log("West inventory:", westBranchInventory);
console.log("East inventory:", eastBranchInventory);
console.log("Central inventory:", centralBranchInventory);`}</pre>
  }
>
  <ul className="m-0 list-disc space-y-1 pl-5">
    <li>Create three branch inventory arrays using <code>[...productionLine]</code>.</li>
    <li>Use <code>pop()</code> on <code>eastBranchInventory</code> and store the removed car.</li>
    <li>Log all three inventories.</li>
    <li>Compare the result with the shared-reference version. Which inventory changes now?</li>
  </ul>
</HandbookChallenge>

---

## 6) One important note: spread is shallow

Spread makes a **new outer container**:

* a new object for `{ ...car }`
* a new array for `[...productionLine]`

But it does **not** magically clone everything inside.

If an array contains objects, spread copies the array, but the objects inside are still the same object references.

For now, that’s fine—and it’s exactly what you’ll use most often.

---

## **Technical Vocabulary**

| Term            | Plain-English meaning                                                | Tiny example                                      |
| :-------------- | :------------------------------------------------------------------- | :------------------------------------------------ |
| reference       | A pointer‑like link to an array or object, not a full copy          | `const otherNums = nums`                          |
| reference type  | A type where variables hold references instead of raw values        | `const car = { make: "Hondord" }`                |
| mutate          | Change an existing array or object in place                         | `car.id = 7`, `productionLine.push(car)`         |
| non‑mutating    | Make a new array/object instead of changing the original            | `const copy = [...productionLine]`               |
| shared reference| Two variables that both point at the same underlying array/object   | `const east = productionLine`                    |
| spread operator | The `...` syntax that makes shallow copies or merges properties     | `const newCar = { ...car, id: nextId }`          |
| shallow copy    | A new outer array/object that still shares its inner objects/arrays | `[...carsWithDetails]`                            |

---

## What to remember

* Primitives copy values. Arrays and objects copy **references**.
* **Mutating** changes the original array/object in place.
* A lot of bugs come from accidentally mutating shared references.
* Use spread for quick shallow copies:
  * objects: `{ ...obj }`
  * arrays: `[...arr]`

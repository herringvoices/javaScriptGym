---
id: JS.VDT.COL
title: Collections · Objects & Arrays (Overview)
short: Why we bundle values into objects and arrays when single variables start to feel repetitive.
---

# Intro to Collections

So far, you’ve stored values one box at a time—titles, page counts, flags. Real apps need to move **groups** of related values around together. That’s what **collections** are for.

In this standard, we’ll keep everything inside a tiny **book library**.

* **Objects** bundle related info about **one thing** (a single book) into key–value pages.
* **Arrays** keep an **ordered list** of things (all the books on a shelf).

You’ll build from loose primitives → book objects → arrays of titles → arrays of book objects, all in one growing script.

---

## One book = many separate variables

Start with a single book described by separate variables. No objects or arrays yet—just primitives.

```js
const book1Title = "The Way of Kings";
const book1Author = "Brandon Sanderson";
const book1Pages = 1007;
const book1IsPaperback = false;

console.log(book1Title);
console.log(book1Author);
console.log(book1Pages);
console.log(book1IsPaperback);
```

<HandbookChallenge
  title="Describe one book with variables"
  hints={[
    <>Each variable should hold one primitive piece of information about the same book.</>,
    <>Titles and authors are strings, page counts are numbers, and paperback status is a boolean.</>,
    <>Use a consistent <code>book1...</code> naming pattern so the relationship is easy to see.</>,
  ]}
  expected={
    <pre className="m-0 whitespace-pre-wrap">{`The Way of Kings
Brandon Sanderson
1007
false`}</pre>
  }
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`const book1Title = "The Way of Kings";
const book1Author = "Brandon Sanderson";
const book1Pages = 1007;
const book1IsPaperback = false;

console.log(book1Title);
console.log(book1Author);
console.log(book1Pages);
console.log(book1IsPaperback);`}</pre>
  }
>
  <p className="m-0">
    Create <code>book1Title</code>, <code>book1Author</code>, <code>book1Pages</code>, and <code>book1IsPaperback</code> for one book, then log each value on its own line.
  </p>
</HandbookChallenge>

---

## Two books = twice as many variables

When your library grows, the separate-variable approach starts to creak. Add a second book by mirroring the pattern.

```js
const book2Title = "Words of Radiance";
const book2Author = "Brandon Sanderson";
const book2Pages = 1087;
const book2IsPaperback = false;
```

<HandbookChallenge
  title="Add a second book"
  hints={[
    <>The second book needs the same four pieces of information as the first.</>,
    <>Keep the data types aligned with the first set of variables.</>,
    <>Use the same naming pattern, but begin each variable with <code>book2</code>.</>,
  ]}
  expected={
    <pre className="m-0 whitespace-pre-wrap">{`The Way of Kings by Brandon Sanderson
Words of Radiance by Brandon Sanderson`}</pre>
  }
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`const book2Title = "Words of Radiance";
const book2Author = "Brandon Sanderson";
const book2Pages = 1087;
const book2IsPaperback = false;

console.log(book1Title, "by", book1Author);
console.log(book2Title, "by", book2Author);`}</pre>
  }
>
  <p className="m-0">
    Declare matching <code>book2...</code> variables for a second book, then log both books’ titles and authors on two lines.
  </p>
</HandbookChallenge>

---

## Spot the pattern (and its pain)

Now that you have two books, notice how similar all these variables are: the names, the shapes, the log statements.

```js
console.log(book1Title, book2Title);
console.log(book1Pages, book2Pages);
```

<HandbookChallenge
  title="Compare the book variables"
  hints={[
    <>A single <code>console.log()</code> can receive more than one value.</>,
    <>Use the existing title variables together, then do the same with the page-count variables.</>,
  ]}
  expected={
    <pre className="m-0 whitespace-pre-wrap">{`The Way of Kings Words of Radiance
1007 1087`}</pre>
  }
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`console.log(book1Title, book2Title);
console.log(book1Pages, book2Pages);`}</pre>
  }
>
  <p className="m-0">
    Log both titles on one line, then both page counts on one line. Pay attention to how much repeated naming you have to manage.
  </p>
</HandbookChallenge>

This chapter uses only primitives on purpose. In the next chapters, you’ll see how **objects** and **arrays** clean this up by grouping all of a book’s fields and all of your books into collections.

---

## Checkpoint A · Primitive book data

By the end of this overview, your editor should look roughly like this (values can differ, but keep the structure and names):

```js
const book1Title = "The Way of Kings";
const book1Author = "Brandon Sanderson";
const book1Pages = 1007;
const book1IsPaperback = false;

const book2Title = "Words of Radiance";
const book2Author = "Brandon Sanderson";
const book2Pages = 1087;
const book2IsPaperback = false;

console.log(book1Title, book2Title);
console.log(book1Pages, book2Pages);
```

You’ll use these primitive variables to build proper book objects in the next chapter.

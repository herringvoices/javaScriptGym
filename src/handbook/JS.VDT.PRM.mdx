---
id: JS.VDT.PRM
title: Variables · Boxes for Data
short: Declare a variable, put a value in it, and change it when the world changes.
---

# Variables: boxes that hold information

So, JavaScript is a **toolbox for manipulating information**—data. To use those tools well, we need **easy labels** for the pieces of data we’re working with. We do this in real life all the time.

Imagine your friend Nick says, “I’m 35 years old.” Your brain makes a little box, writes **Nick’s age** on the label, and tucks that fact inside. Later, if someone asks, “How old is Nick?” you look back in your memory, find the box labeled “Nick’s age,” and read what’s inside.

That’s a **variable**: a labeled box that holds a value. The **label** is the variable’s name; the **value** is what’s inside.

## Build the box (declare), then peek inside

We build a box with the keyword `let`—a special, reserved word in JavaScript. Keywords are like command words: when you use one, the language does a specific built‑in job. In this case, `let` tells JavaScript to make a new variable.

### Name the box (the label)

After we build our box, we need to create a label for it—the variable’s name. Choose names that **say what’s inside** and still make sense a week from now. Imagine leaving a sticky note for future‑you and your teammates.

**Bad → Better (and why)**

| Bad                                                  | Better            | Why                     |
| ---------------------------------------------------- | ----------------- | ----------------------- |
| `nn`                                                 | `nickName`        | Says what it holds      |
| `c`                                                  | `favoriteColor`   | Specific, not cryptic   |
| `theNameOfTheUserWhoIsCurrentlyLoggedIn`             | `currentUserName` | Too wordy; hard to scan |
| `theTitleThatIsDisplayedAtTheTopOfTheCurrentWebpage` | `pageTitle`       | Concise and clear       |

Use **camelCase** for names:

* start lowercase: myName
* New words start uppercase: favoriteColor
* no spaces; don’t start with a number; avoid reserved words

Let's see an example:

```js
let myName;
console.log(myName); // undefined (box exists, but it’s empty)
```

## Put something in the box (assign)

After building your box, you use the **assignment operator** (the equals sign, `=`) to put a value inside it.

For example:

```js
let myName; // build the box (declare the variable)
myName = "Nick"; // put a value inside (assign)
console.log(myName); // "Nick"
```

The most important rule to remember is: **JavaScript always solves the right-hand side first.** It looks at everything to the right of the `=`—whether it's a simple number, a math problem, or other variables—and works until it has one single, final value.

Then, it takes that single result and stores it in the one variable (the "box label") on the left-hand side. The left side must always be a single box name, ready to receive the value.

For example, if the value you're storing is text, it must be wrapped in quotes (like "Nick").

Usually we assign variables a value when we first declare them. This is called **initializing**.

```js
let myName = "Nick";
console.log(myName); // "Nick"
```

<HandbookChallenge
  title="Create the viewer name"
  hints={[
    <>A declared variable with no assigned value evaluates to <code>undefined</code>.</>,
    <>The first log has to happen before the assignment if you want to observe that empty state.</>,
    <>Remember that <code>let viewerName;</code> declares the variable without assigning a value.</>,
  ]}
  expected={
    <pre className="m-0 whitespace-pre-wrap">{`undefined
Milo`}</pre>
  }
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`let viewerName;
console.log(viewerName); // undefined
viewerName = "Milo";
console.log(viewerName);`}</pre>
  }
>
  <p className="m-0">
    Start the Movie Night Planner with a variable named <code>viewerName</code>. Declare it without a value and log it. Then assign your own name as a string and log it again so you can see the value change.
  </p>
</HandbookChallenge>

---

## Changing what’s in the box (reassignment)

When you reassign a variable, you do not use the `let` keyword again. You’ve already built the box; now you’re just changing what’s inside. Using `let` again would tell JavaScript to try and build a new box with the same label, which usually results in an error.

```js
// 1. Build the box (declare) and put something in (initialize)
let currentScore = 100;
console.log(currentScore); // 100

// 2. Change what's in the box (reassign)
// Notice: NO "let" keyword!
currentScore = 150;
console.log(currentScore); // 150

// This would cause an error:
// let currentScore = 150; // ❌ SyntaxError: 'currentScore' has already been declared
```

<HandbookChallenge
  title="Update the planning details"
  hints={[
    <>Use <code>let</code> for values you plan to change later.</>,
    <>Reassignment uses the existing variable name and <code>=</code>; you do not write <code>let</code> a second time.</>,
    <>Keep each piece of information in its own variable so you can reuse and log them together.</>,
  ]}
  expected={
    <pre className="m-0 whitespace-pre-wrap">{`chill
<your name> <your snack> <new mood>`}</pre>
  }
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`let viewerName;
console.log(viewerName); // undefined
viewerName = "Milo";
console.log(viewerName);

let currentMood = "chill";
console.log(currentMood); // chill
currentMood = "excited";
let snack = "kettle corn";
console.log(viewerName, snack, currentMood);`}</pre>
  }
>
  <Callout type="note" title="Starting point">
    <p className="m-0">Keep the <code>viewerName</code> code from the previous challenge in your editor.</p>
  </Callout>

  <p className="m-0">
    Add <code>currentMood</code> with a starting value of <code>"chill"</code> and add a <code>snack</code> variable. Log the starting mood, reassign <code>currentMood</code> to a different string, then log <code>viewerName</code>, <code>snack</code>, and <code>currentMood</code> together.
  </p>
</HandbookChallenge>

Now, sometimes we don’t want a value to change. Think about a person’s birth date, the number of days in a week, or the name of your website. These things aren’t supposed to be reassigned later in the code.

When that’s true, we use a different keyword when we first declare our variable: `const`.

`const` is short for constant. It’s like you build a box, put something inside, and immediately lock the box and throw away the key. You can peek inside anytime, but you can never change what’s in there.

This gives us two keywords to declare a variable, depending on our intention:

* **`const` (constant):** Use this when you don’t plan to ever change what’s inside that box.
* **`let`:** Use this when the value might change later (like a `currentScore`).

```js
const siteName = "JS Handbook";
console.log(siteName);
// siteName = "New Name"; // ❌ TypeError if you try to reassign

let favColor = "pink";
favColor = "teal";         // ✅ value changed
console.log(favColor);
```

<HandbookChallenge
  title="Separate fixed and changing values"
  hints={[
    <>Choose <code>const</code> when you do not plan to reassign a variable.</>,
    <>Keep changing values such as <code>currentMood</code> declared with <code>let</code>.</>,
    <>Only reassign the variable that is supposed to change. A <code>const</code> assignment cannot be replaced later.</>,
  ]}
  expected={
    <pre className="m-0 whitespace-pre-wrap">{`living room CouchFlix
chill
energized`}</pre>
  }
  solution={
    <pre className="m-0 whitespace-pre-wrap">{`let viewerName;
console.log(viewerName);
viewerName = "Milo";
console.log(viewerName);

let snack = "kettle corn";
let currentMood = "chill";
currentMood = "excited";
console.log(snack, currentMood);

const location = "living room";
const streamingService = "CouchFlix";
console.log(location, streamingService);

currentMood = "energized";
console.log("chill");
console.log(currentMood);

// location = "backyard"; // TypeError if you uncomment this line`}</pre>
  }
>
  <Callout type="note" title="Starting point">
    <p className="m-0">Leave <code>viewerName</code>, <code>snack</code>, and <code>currentMood</code> from previous challenges in your editor.</p>
  </Callout>

  <div className="space-y-2">
    <p className="m-0">
      Add <code>const location</code> and <code>const streamingService</code> for details that should stay fixed. Log both constants. Then set <code>currentMood</code> to <code>"chill"</code>, log it, reassign it to <code>"energized"</code>, and log it again so the difference between fixed and changing values is visible.
    </p>
    <p className="m-0">
      Optional: temporarily try to reassign <code>location</code> to see the TypeError, then comment that line out.
    </p>
  </div>
</HandbookChallenge>
<br/>

<Callout type="tip" title="Movie Night Planner checkpoint">
  <p className="m-0">Before you move on to strings, tidy your file so it at least contains the core variables below (customize the values to match your story). Later chapters assume this baseline exists.</p>
  <pre className="m-0 whitespace-pre-wrap">{`let viewerName = "Milo";
let currentMood = "excited";
const location = "living room";
const streamingService = "CouchFlix";

console.log(viewerName, currentMood);
console.log(location, streamingService);`}</pre>
  <p className="m-0">If your editor looks wildly different, clear it and paste this snippet before continuing.</p>
</Callout>

---

## Technical Vocabulary

| Term                  | Plain-English meaning                 | Tiny example                                |
| :-------------------- | :------------------------------------ | :------------------------------------------ |
| variable              | Labeled box that holds a value        | `let myName;`                               |
| identifier (name)     | The label on the box                  | `myName`                                    |
| declare / declaration | Make the box                          | `let myName;`                               |
| assign / assignment   | Put a value in the box with `=`       | `myName = "Nick";`                          |
| initialize            | Declare **and** assign in one step    | `let myName = "Nick";`                      |
| reassign              | Replace the value later               | `favColor = "teal";`                        |
| keyword               | Reserved command word in JS           | `let`, `const`                              |
| `const`               | A variable you don’t plan to reassign | `const siteName = "JS Handbook";`           |
| `let`                 | A variable you might reassign         | `let favColor = "pink";`                    |
| assignment operator   | The `=` that assigns values           | `x = y;`                                    |
| camelCase             | Naming style for variables            | `favoriteColor`                             |
| `undefined`           | The box exists, but it’s empty        | after `let myName;` → `console.log(myName)` |

---

## Wrap‑up

You learned how to **declare** a variable, **name** it clearly, **assign** a value with `=`, and when to choose **`const`** vs **`let`**. You also saw `undefined`—what shows up when the box exists but nothing’s inside yet.

**Up next: *Data Types*.** That’s just “what kinds of things can go in the box.” We’ll start with text in quotes, then branch out.

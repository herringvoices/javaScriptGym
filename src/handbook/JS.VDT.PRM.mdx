---
id: JS.VDT.PRM
title: Variables · Boxes for Data
short: Declare a variable, put a value in it, and change it when the world changes.
---

# Variables: boxes that hold information

So, JavaScript is a **toolbox for manipulating information**—data. To use those tools well, we need **easy labels** for the pieces of data we’re working with. We do this in real life all the time.

Imagine your friend Nick says, “I’m 35 years old.” Your brain makes a little box, writes **Nick’s age** on the label, and tucks that fact inside. Later, if someone asks, “How old is Nick?” you look back in your memory, find the box labeled “Nick’s age,” and read what’s inside.

That’s a **variable**: a labeled box that holds a value. The **label** is the variable’s name; the **value** is what’s inside.

## Build the box (declare), then peek inside

We build a box with the keyword `let`—a special, reserved word in JavaScript. Keywords are like command words: when you use one, the language does a specific built‑in job. In this case, `let` tells JavaScript to make a new variable.

### Name the box (the label)

After we build the box, we need to create a label for it—the variable’s name. Choose names that **say what’s inside** and still make sense a week from now. Imagine leaving a sticky note for future‑you and your teammates. nickName beats nn; favoriteColor beats c.

**Bad → Better (and why)**

| Bad                                                  | Better            | Why                     |
| ---------------------------------------------------- | ----------------- | ----------------------- |
| `nn`                                                 | `nickName`        | Says what it holds      |
| `c`                                                  | `favoriteColor`   | Specific, not cryptic   |
| `theNameOfTheUserWhoIsCurrentlyLoggedIn`             | `currentUserName` | Too wordy; hard to scan |
| `theTitleThatIsDisplayedAtTheTopOfTheCurrentWebpage` | `pageTitle`       | Concise and clear       |

Use **camelCase** for names:

* start lowercase: myName
* New words start uppercase: favoriteColor
* no spaces; don’t start with a number; avoid reserved words

```js
let myName;
console.log(myName); // undefined (box exists, but it’s empty)
```

## Put something in the box (assign)

After building your box, you use the **assignment operator** (the equals sign, `=`) to put a value inside it.

For example:

```js
let myName; // build the box (declare the variable)
myName = "Nick"; // put a value inside (assign)
console.log(myName); // "Nick"
```

The most important rule to remember is: **JavaScript always solves the right-hand side first.** It looks at everything to the right of the `=`—whether it's a simple number, a math problem, or other variables—and works until it has one single, final value.

Then, it takes that single result and stores it in the one variable (the "box label") on the left-hand side. The left side must always be a single box name, ready to receive the value.

For example, if the value you're storing is text, it must be wrapped in quotes (like "Nick").

Usually we assign variables a value when we first declare them. This is called **initializing**.

```js
let myName = "Nick";
console.log(myName); // "Nick"
```

---

<HandbookChallenge
title="Hello, box"
hints={[
"Declare first, then log. After that, assign, then log again.",
"Words need quotes.", <pre key="peek" className="m-0 whitespace-pre-wrap">{`let myName;\nconsole.log(myName);\nmyName = "Ada";\nconsole.log(myName);`}</pre>,
]}
answers={ <pre className="m-0 whitespace-pre-wrap">{`let myName;\nconsole.log(myName); // undefined\nmyName = "Your Name";\nconsole.log(myName);`}</pre>
}
>

  <p className="m-0">
    Make a variable called <code>myName</code>. Log it (you should see <code>undefined</code>). Then assign your real name and log it again.
  </p>

  <p className="mt-3 mb-1 text-slate-300"><strong>Expected console output:</strong></p>
  <pre className="m-0 whitespace-pre-wrap">{`undefined\nYour Name`}</pre>
</HandbookChallenge>

---

<HandbookChallenge
title="Favorite things"
hints={[
"Make one variable per favorite.",
"Use clear names in camelCase, like <code>favColor</code>.", <pre key="peek" className="m-0 whitespace-pre-wrap">{`let favColor = "pink";\nconsole.log(favColor);`}</pre>,
]}
answers={ <pre className="m-0 whitespace-pre-wrap">{`let favColor = "pink";\nlet favSong = "Thunder Road";\nlet favBook = "The Hobbit";\nlet favMovie = "Spider-Verse";\nlet favFruit = "Mango";\nconsole.log(favColor, favSong, favBook, favMovie, favFruit);`}</pre>
}
>

  <p className="m-0">
    Create variables for your favorites: <code>favColor</code>, <code>favSong</code>, <code>favBook</code>, <code>favMovie</code>, <code>favFruit</code>. Assign each to a word and log them.
  </p>

  <p className="mt-3 mb-1 text-slate-300"><strong>Possible console output:</strong></p>
  <pre className="m-0 whitespace-pre-wrap">{`pink Thunder Road The Hobbit Spider-Verse Mango`}</pre>
  <Note>
    Be sure you log the <strong>variables</strong> (like <code>console.log(favColor)</code>) and not
    hard‑coded strings (like <code>console.log("pink")</code>).
  </Note>
</HandbookChallenge>

## Changing what’s in the box (reassignment)

Why change a value later? Because once a site is live, users change things. They click buttons, type into inputs, and flip settings. Your code needs to respond. Sometimes you keep the same label but replace what’s inside the box—that’s **reassignment**.

We use two keywords to declare a variable:

* **`const`** when you don’t plan to change what’s inside that box
* **`let`** when the value might change later

```js
const siteName = "JS Handbook";
console.log(siteName);
// siteName = "New Name"; // ❌ TypeError if you try to reassign

let favColor = "pink";
favColor = "teal";         // ✅ value changed
console.log(favColor);
```

<HandbookChallenge
title="Won’t change / might change"
hints={[
"Pick a label that shouldn’t change (like a site or course name) — that’s a const.",
"Pick a label that might change (like a favorite) — that’s a let.", <pre key="peek" className="m-0 whitespace-pre-wrap">{`const siteName = "JS Handbook";\nconsole.log(siteName);\n// siteName = "New Name"; // ❌ TypeError\n\nlet favColor = "pink";\nconsole.log(favColor);\nfavColor = "teal";\nconsole.log(favColor);`}</pre>,
  ]}
  answers={     <pre className="m-0 whitespace-pre-wrap">{`const siteName = "JS Handbook";\nconsole.log(siteName);\n// siteName = "New Name"; // TypeError: Assignment to constant variable.\n\nlet favColor = "pink";\nconsole.log(favColor);\nfavColor = "teal";\nconsole.log(favColor);`}</pre>
}
>

  <div className="space-y-2">
    <p className="m-0">
      Make one variable you won’t change (as a <code>const</code>) and one you might change (as a <code>let</code>). Log each. Then change the <code>let</code> and see the new value.
    </p>
    <p className="m-0">
      Optional: try to change the <code>const</code> line to see the error—then comment that line back out.
    </p>
  </div>

  <p className="mt-3 mb-1 text-slate-300"><strong>Expected console output:</strong></p>
  <pre className="m-0 whitespace-pre-wrap">{`JS Handbook\npink\nteal`}</pre>
  <p className="m-0 text-slate-400"><em>If you try to reassign the const:</em> <code>TypeError: Assignment to constant variable.</code></p>
</HandbookChallenge>

---

## Technical Vocabulary

| Term                  | Plain-English meaning                 | Tiny example                                |
| :-------------------- | :------------------------------------ | :------------------------------------------ |
| variable              | Labeled box that holds a value        | `let myName;`                               |
| identifier (name)     | The label on the box                  | `myName`                                    |
| declare / declaration | Make the box                          | `let myName;`                               |
| assign / assignment   | Put a value in the box with `=`       | `myName = "Nick";`                          |
| initialize            | Declare **and** assign in one step    | `let myName = "Nick";`                      |
| reassign              | Replace the value later               | `favColor = "teal";`                        |
| keyword               | Reserved command word in JS           | `let`, `const`                              |
| `const`               | A variable you don’t plan to reassign | `const siteName = "JS Handbook";`           |
| `let`                 | A variable you might reassign         | `let favColor = "pink";`                    |
| assignment operator   | The `=` that assigns values           | `x = y;`                                    |
| camelCase             | Naming style for variables            | `favoriteColor`                             |
| `undefined`           | The box exists, but it’s empty        | after `let myName;` → `console.log(myName)` |

---

## Wrap‑up

You learned how to **declare** a variable, **name** it clearly, **assign** a value with `=`, and when to choose **`const`** vs **`let`**. You also saw `undefined`—what shows up when the box exists but nothing’s inside yet.

**Up next: *Data Types*.** That’s just “what kinds of things can go in the box.” We’ll start with text in quotes, then branch out.
